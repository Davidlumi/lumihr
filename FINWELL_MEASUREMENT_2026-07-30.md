# M7 · FINWELL — MEASUREMENT · 2026-07-30 · READ-ONLY
### No repair designed · no draw computed · no migration written · the frozen sibling not touched, not staged.
### This sheet is the only write.

---

# THE HEADLINE — M7 IS WITHDRAWN. THERE IS NO CONTRADICTION.

**The GM reconciliation ranked M7 as a MASKED row: *"the exact-on-target achieved value sits on top of a
108-org contradiction."* That reading was wrong, and this measurement withdraws it.**

The two metrics are **deliberately different constructs, from the same source, and the register says so
in terms.** The 108 orgs are not a defect — **they are the source's own finding, reproduced to the decimal.**

| | REW263_WEL_FINWELL (ladder) | REW26_WEL_FINWELL (sibling, FROZEN) |
|---|---|---|
| question | *"Is there a documented financial wellbeing **strategy** beyond ad hoc provision?"* | *"Do you offer a financial wellbeing **programme**?"* |
| help | *"…education, tools, or guidance. **Debt advice and coaching are asked separately.**"* | *"Include education, tools **or coaching** — not just signposting."* |
| register anchor | *"Financial wellbeing **strategy/policy: 15%** all orgs"* | *"**64%** offer a financial wellbeing **programme/activity** (broad any-programme measure, CIPD S29)"* |
| source | CIPD Reward Survey Feb 2026 (n=1,059) | **the same survey** |
| grade | A | A |
| live | **33/220 = 15.00%** | **141/220 = 64.09%** |

**THE ARITHMETIC THAT SETTLES IT:**
```
ladder   33/220 = 0.1500   vs anchor 15%   (documented strategy)
sibling 141/220 = 0.6409   vs anchor 64%   (any programme)
the 108 108/220 = 0.4909   vs the SOURCE's own gap, 64 − 15 = 49pp     ← EXACT
```
**The 108 orgs ARE the 49-point gap CIPD measured between "offers a programme" and "has a documented
strategy". The seed reproduces both anchors to within 0.09pp and the gap exactly.**

The register already anticipated the confusion this pass fell into. The ladder's own notes read:
*"**NB earlier 'finwell 64%' was the DIFFERENT metric** (agree good-benefits among FW-policy orgs). This
is the strategy-exists prevalence = 15%"*; and the sibling's anchor records a **ruled relabelling**:
*"RELABELLED ruled 2026-07-16: previously mislabelled 'have financial wellbeing policy/strategy';
attachment ruled correct per the standing register reconciliation (audit mis-map **OVERTURNED**)."*
**The conflation M7 made had already been made once, caught, and ruled against on 2026-07-16.**

---

# THE MATERNITY-PIN VERDICT

**NO — and it is not the sick-chain shape either. It is a third thing.**

| shape | test | FINWELL |
|---|---|---|
| **maternity pin (M3/M4)** | two targets arithmetically irreconcilable with coherence; **no repair exists** | **NO.** Both targets are simultaneously satisfied *today* — 15.00% and 64.09% against 15% and 64%. Nothing is pinned against anything. |
| **sick chain (M1/T8)** | a base/coherence defect with a repairable direction | **NO.** There is no defect to repair; the base is correct and the constructs are distinct by design. |
| **definitional (this)** | the "contradiction" is two different questions answered consistently | **YES.** |

The superficial resemblance to maternity is that *any* repair would break a target (see below). But the
maternity pin was *"repair the incoherence and the anchors fail"*; here it is **"there is no incoherence,
and repairing the non-problem would destroy two grade-A anchors that currently both hold."**

---

# THE FOUR DIRECTIONS, MEASURED

**(a) Repair the ladder toward the sibling** — move the 108 off "No".

| variant | ladder positive | vs target 0.150 | verdict |
|---|---|---|---|
| all 108 move | 141/220 = 0.6409 | **+49.1pp** | **FAIL** |
| only the 62 "unexplained" | 95/220 = 0.4318 | **+28.2pp** | **FAIL** |
| only the 46 "explained" | 79/220 = 0.3591 | **+20.9pp** | **FAIL** |

**Donor shape: Documented 30 / Ad hoc 3 = 91% / 9% — DEGENERATE**, worse than M1's. Drawing 108 on it
puts **~98 orgs on "Documented strategy"**, asserting that **131 of 220 employers (60%) have a documented
financial-wellbeing strategy** against a grade-A source saying 15%. **That is inventing data**, and it
destroys the anchor it was meant to serve. **Rejected on its own arithmetic.**

**(b) Repair the sibling toward the ladder** — move the 108 from Yes to No on the **frozen** metric.

| variant | sibling Yes | vs frozen 0.6409 | verdict |
|---|---|---|---|
| all 108 move | 33/220 = 0.1500 | **49.09pp drift** | **BREACH** (line 0.1pp) |
| only the 62 | 79/220 = 0.3591 | **28.18pp drift** | **BREACH** |

**Full cost if pursued:** the ruled **unfreeze → reshape → re-freeze** cycle (batch-2 SALSAC precedent) —
lift the freeze, reshape, re-freeze at the new distribution, with the tier-1 gate re-armed and asserted at
each step against an **absolute 0.1pp** line, `frozen_targets.json` being the single source both
`qa_plausibility` and `qa_reseed` derive from. **And it would move a metric whose anchor the register
records as VERIFIED — *"seeded 64% Yes — EXACT MATCH to CIPD 64%. Confirmed correct."* Rejected.**

**(c) Declare the pair — and the direction of the pair is the whole question.**
```
child = ladder-provision (33) SUBSET parent = sibling-Yes (141)   ->  0 violations
EQUALITY relation (child == parent)                              -> 108 violations
```
**The cross-tab is perfectly nested: 33 ⊂ 141, with ZERO counter-flow** (no org claims a strategy while
denying a programme). **A `subset_orgs` pair in the natural direction passes today at zero.** The "108
violations" figure exists only under an *equality* relation — which asserts that offering a programme and
having a documented strategy are the same thing, i.e. the very conflation the register overturned.

**(d) THE FINDING — the constructs differ by design, and the evidence is graded.**
Coaching / debt-advice provision (services the ladder **explicitly excludes** and the sibling
**explicitly includes**), across the three groups:

| group | n | has coaching or debt advice |
|---|---|---|
| ladder-provision | 33 | **88%** |
| **the 108** | 108 | **43%** |
| sibling-No | 79 | **11%** |

**Monotone.** The 108 are a real middle tier, not noise. **46 of the 108 (43%) hold coaching or debt
advice — for them, "No" on the ladder and "Yes" on the sibling is DEFINITIONALLY CORRECT**, because the
ladder's help text excludes exactly those services. The remaining 62 offer education/tools without a
documented strategy — **also coherent in the real world, and exactly the population CIPD's 49-point gap
describes.**

---

# THE ONE REAL RESIDUE — a construct mismatch the seed currently hides

`REW263_WEL_FINWELL` has **no `positive_from`**, so the gate's rung-1 cut counts **"Ad hoc provision" as
positive**, while the entry's own `target_semantics` says *"positive pole = has documented strategy"*.
**This is the M6/FAM_009 pattern.** It does not bite today only because the "Ad hoc" rung holds **3 orgs**:

```
current cut (Ad hoc counts positive)   33/220 = 0.1500  ->  +0.00pp
target semantics (Documented only)     30/220 = 0.1364  ->  -1.36pp   PASS
```

**Two things follow.** First, the correct fix here is a **construct correction, not a coherence repair**:
setting `positive_from = 'Documented strategy'` makes the gate measure what the target means, and it still
passes at −1.36pp. Second — **the "Ad hoc provision" rung holding 3 is itself implausible**: if 64% offer a
programme and 15% have a strategy, the ad-hoc population is ~49%, not 1.4%. **The under-populated middle
rung is what keeps the unset-`positive_from` mismatch invisible.** Repopulating it without first setting
`positive_from` would swing the gate from 15% to 64% against a 15% target.

---

# EVIDENCE DISCIPLINE

**The definitional argument is EXTERNAL, not seed-internal** — and that is what distinguishes it from T8's
SICK_005 reasoning. It rests on (i) the two questions' own wording and help text, (ii) **two grade-A
register cells citing the same CIPD Reward Survey Feb 2026 for two different measures**, and (iii) a
**ruled relabelling of 2026-07-16** that already overturned this exact conflation. The seed is the thing
being *checked* against those sources, not the evidence for them.
The coaching/debt-advice gradient **is** seed-internal (FINCOACH and DEBTADVICE are seeded metrics) — it
corroborates the split but does not carry it alone.

**A method note, recorded because it nearly became the finding:** my first definitional test returned
**0% across all three groups including both controls**. That was not evidence — it was a broken predicate
(`value == 'Yes'`, when these metrics use `'Yes funded advice'`, `'Signposted (e.g. MaPS)'`, etc.). **A
null result is only evidence if the instrument was working**; the controls reading 0% is what exposed it.

---

# RECOMMENDATION

1. **WITHDRAW M7.** It is not a masked row and there is no contradiction. Record the withdrawal against
   the GM reconciliation, as M5's circularity charge was withdrawn.
2. **BUILD NO REPAIR.** Every direction is rejected on its own arithmetic: (a) +20.9 to +49.1pp on a
   degenerate donor, (b) a 28–49pp breach of a 0.1pp frozen line on a verified anchor.
3. **DECLARE the `subset_orgs` pair** (child = ladder-provision, parent = sibling-Yes,
   `parent_answered_only`). It reads **0 violations today** and would catch a genuine future inversion —
   a guard that costs nothing and prevents this misreading recurring. *Config class, David's ruling.*
4. **SET `positive_from = 'Documented strategy'`** so the gate measures the target's own semantics
   (−1.36pp, PASS). *Data class, David's ruling — the M6/FAM_009 precedent.*
5. **ANCHOR QUEUE:** the "Ad hoc provision" rung at 3 orgs against an implied ~49% ad-hoc population.
   **This is a seed-texture question requiring a source read, not a draw** — the CIPD Reward Survey
   Feb 2026 (n=1,059) would need checking for whether it splits ad-hoc from none. **Sequenced AFTER (4)**:
   repopulating that rung before the cut is corrected would break the target.

---

# STATE
Read-only. Live answers book `11f574a0735e1b61` (89,321 rows) unchanged · `generated_marginals.json`,
`frozen_targets.json`, `structured_bases.json` unchanged · the frozen sibling not touched, not staged,
not rehearsed against · no migration written · this sheet is the only write.
