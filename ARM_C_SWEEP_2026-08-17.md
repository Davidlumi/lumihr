# Arm C sweep — fixture divergence, full candidate set

Twelve gates. 120 candidates. `direction` reads from the FIXTURE's point of view:
**fixture-omits** = production always passes something the gate never does;
**fixture-invents** = the gate passes something production never sends.
`confidence` and `prints` are the analysing agent's assessment. Three rows were
verified independently and are named in `PHASE2_REPORT_2026-08-17.md` §3.


## qa_commentary.py — 10 candidates

| line | check | direction | divergence | consequence | conf | prints |
|---|---|---|---|---|---|---|
| 61 | every number in %d generated commentaries exists in its input (Section A) | fixture-omits | `dim` is only ever all/industry/fte_band. Production's endpoint explicitly whitelists five dims — app.py:7253 `dim = body.get("cut") if body.get("cut") in ("all","industry","fte_band","twin","group") else "all"` (and identically at 7301 for /save) — and the metric page's own selector emits both: web/js/app.js:1802 renders the `twin` option and 1787-1789 renders one `group::<group_id>` option per saved peer group, bot | `tb = twin_blocks_if_needed(conn, org, cut) if cut["dim"] in ("twin","group") else None` (app.py:7173) never fires, so `tb` is None on every single payload the gate builds and the single-key twin-blocks dict `{qid: tb.get(qid)}` (app.py:7175) is NEVER passed to assemble_card — assemble_card always receives the literal None. positions.py:83-85 `block_for` returns `(twin_blocks or {}).get("main")` on twin/group cuts, a | confirmed | **yes** |
| 208 | injected instructions do not alter the verdict or figures (still framed behind)  | fixture-omits | Section F injects only into `definition` and `metric` — both lumi-authored library strings (q.text / q.display_title from the questions bank). The one MEMBER-authored free-text string that reaches this payload is `cut_label`, and the gate never produces the shape that carries it: on a group cut, app.py:7171 sets `cut["label"] = row["name"]` from the member's own peer_groups row, positions.py:85 returns that as the bl | The only untrusted, user-controlled string in the commentary payload is never attacked. It is interpolated verbatim into the deterministic prose at claude_api.py:1305-1306 ("Fewer than 5 organisations in this peer group (%s) answered"), 1331, 1342-1348 and 1366, and it is serialised into the model's user message by generate_metric_commentary (claude_api.py:1420 `json.dumps(payload)`). A peer group named "Ignore previ | confirmed | **yes** |
| 75 | sparse input (you/most-common/n only): no invented statistic, £, or 'typical' fi | fixture-omits | The hand-built payload omits `n_real`, which production ALWAYS sets — app.py:7206 `"n": card["n"], "n_real": card.get("n_real", 0)` is unconditional on every payload build_commentary_payload returns. It also omits `cls`, `direction`, `category` and `metric_type`, all of which production sets unconditionally at app.py:7214-7217. | validate_commentary fails closed on the missing key — claude_api.py:1237-1239 `if n_real is None: return False, "composition data (n_real) missing from payload — fail closed"`. generate_metric_commentary runs that check on its OWN fallback first (claude_api.py:1406-1411) and, on failure, throws the real narrative away and substitutes the belt-and-braces stub. Executed: the text this check actually scans is `'Sparse t | confirmed | **yes** |
| 86 | numeric without supplied quartiles: no median/quartile fabricated | fixture-omits | Same omission: no `n_real`, which app.py:7206 always supplies. Also no `cls`/`direction`, so `no_direction` (claude_api.py:1320) can never be evaluated on real values. | Identical fail-closed collapse. Executed: the scanned text is `'d. See the chart above for this comparison. — —'`. The `stance == "in line"` branch of _deterministic_commentary (claude_api.py:1391-1396) — the branch that actually emits a percentile readout and would append " The peer median is %s." if peer_median_display were present — is never reached, so the check named for median/quartile fabrication scans four wo | confirmed | **yes** |
| 265 | cache key (payload hash) changes when underlying data changes | fixture-omits | The fixture hashes `j(payload)` alone. Production hashes `j(payload) + "\|" + claude_api.COMMENTARY_GEN_VERSION` — app.py:7260-7261 on the read/generate path and app.py:7314 on the save path, with COMMENTARY_GEN_VERSION = "2026-06-18.sdk-opus48-v4-malformguard" (claude_api.py:830). The version salt is never in the string the gate hashes. | The generator-version half of the cache key is unexercised. The check verifies only that the key moves when the DATA moves; it is structurally incapable of noticing if COMMENTARY_GEN_VERSION were dropped from, or never re-bumped in, the hash construction. That is the exact regression that would leave stale AI or edited commentary cached across a prompt/generator change — app.py:7264-7270 returns the stored `metric_co | confirmed | **yes** |
| 146 | lower_is_better with low (favourable) value resolves stance=ahead / low-is-good  | fixture-invents | The fixture passes the polarity literal "lower_is_better" and then writes the resulting stance ("ahead") back onto a payload whose `direction`/`polarity` keys still carry OT_04_b14623a6's real value, higher_is_better. Production derives stance ONLY from those same keys — app.py:7195-7201 uses `_dir = _mc.get("direction")` and falls back to the card's `pol` — so stance and direction are mutually consistent in every pa | Both checks assert over a payload production cannot construct: stance='ahead' at percentile=20.0 with direction='higher_is_better' is arithmetically impossible under _commentary_stance (app.py:7024, `adj = 100 - percentile if polarity == "lower_is_better" else percentile`), which would return 'behind' for that real payload. The polarity-inversion protection the check advertises covers a direction value that appears n | confirmed | no |
| 117 | real suppressed cut detected (dynamic: %s on %s) | both | Two divergences at once. (a) The fixture rewrites the org row's `industry` to a foreign sector while leaving `org_id` as the demo org's — request.state.org (app.py:470) is always `dict(SELECT * FROM orgs)` and its industry always matches its own org_id. (b) `value` is None here and at all eight build_commentary_payload call sites, so the `if dim == "industry" and not value: value = org.get("industry")` default (app.p | The explicit-cut_value branch of build_commentary_payload is never exercised anywhere in the file — no call passes a non-None `value`, so a regression in how an explicitly supplied sector/band/group_id is threaded into the cut would be invisible. And the only suppressed payload the entire gate produces — which both Section B's three checks and Section V's suppression-breach check depend on — is built from an org dict | likely | **yes** |
| 47 | whole file — no check reaches the endpoint layer | fixture-omits | Production's `user` is the full users row returned by require_user(request) (carrying user_id, email, role, org_id); the fixture is a one-key dict. Inside build_commentary_payload this is inert — make_entitled (app.py:734-737) ignores both arguments and returns `lambda q: True`. The load-bearing divergence is what surrounds it: the gate calls the helper directly and never issues a single HTTP request, so nothing betw | Everything the endpoint does around the payload is unexercised: the five-value dim whitelist and its silent coercion to "all" (app.py:7253/7301), require_ai's AI_COMMENTARY gate, _ai_generation_or_429's spend limiter, the `peek` short-circuit that must return parts=None without spending a generation (app.py:7275-7276), the cached-row return path (7264-7270), and the `caveats` object. It also means /api/metric-comment | likely | **yes** |
| 52 | every number in %d generated commentaries exists in its input / legal-adjacent m | fixture-invents | The gate samples candidate qids from `visible_questions()` — the platform-wide library (333 live metrics). Production's build_commentary_payload resolves the qid through `org_visible_questions(org).get(qid)` (app.py:7156) and returns None (endpoint 404) for anything outside it; org_visible_questions (app.py:365-385) strips module metrics for non-tronc sectors and applies data/sector_scopes.json. | Any sampled qid outside the org's visible set yields payload=None and is silently `continue`d (line 63) — and because `by_type` keeps only the FIRST qid per (type, sub_power) pair, losing that one qid drops its whole type/domain bucket from the sample rather than falling back to the next candidate. Measured on the current fixture the harm is latent, not live: the demo org is Retail & Consumer Goods, which is in TRONC | speculative | no |
| 350 | same sentence with real n=10 is NOT composition-blocked (no over-block) | fixture-invents | `n_real` is overwritten with a literal 10 while `n` keeps ALLOW_02's real value from the same payload. Production always derives both from the same assembled card in one pass (app.py:7206, `card["n"]` and `card.get("n_real", 0)`), so n_real is bounded by n by construction; the fixture can and does set them independently. | The over-block probe runs on a (n, n_real) pair that need not satisfy n_real <= n, so it establishes only that the composition rule reads the key — not that it reads a pair the engine can actually produce. The composition-conditional attribution gate is also tested on a single hardcoded qid (ALLOW_02) on a single hardcoded "all" cut, so the way n_real varies per cut (the whole point of a rule that 'relaxes itself as  | speculative | no |

## qa_domain_summary.py — 6 candidates

| line | check | direction | divergence | consequence | conf | prints |
|---|---|---|---|---|---|---|
| 60 | SECTION A — every domain x strategy on/off x cut ships a VALID, present summary  | fixture-omits | The fixture only ever passes cut dims 'all' and 'industry', as bare 2-key {dim,value} literals. Production (POST /api/domain-summary, app.py:7344) accepts dim ∈ all\|industry\|fte_band\|twin\|group straight from the client, and for dim='group' the route ADDS cut['label'] and cut['criteria'] from the peer_groups row (app.py:7358-7359); twin_blocks_if_needed then injects cut['match_count'] and cut['too_small'] (app.py: | Because dim is never twin/group, `tb` at app.py:7062 is always None, so pos.position_items / pos.prevalence_items / pos.practice_position_items are never called from this gate with a real twin-blocks dict — nor with the `{}` all-suppressed dict a below-suppression-floor group returns. This is the same hard-coded-absent-twin-block shape as the qa_overview:73 reference case. It also means _cut_peer_n's twin and group b | confirmed | **yes** |
| 52 | SECTION A/B/C/D — the whole strategy-on axis (alignment, strategy_aim, mix_note) | fixture-omits | any_classified_row (demo_org.py:47-56) returns `SELECT * FROM orgs WHERE classified=1 AND submission_complete=1 ORDER BY org_id LIMIT 1` — org_id 00000830-3d69-7fd0-0000-f7412b5cb6c9, which has NO row in org_strategy. Exactly one org of 270 in the store has a strategy row (5e67fa8c-84b2-4be7-9f59-8556bbd6b6e7, completed_at set), and it is not the one the ORDER BY picks. strategy_for_engine returns None with no row, s | payload['alignment'], payload['strategy_aim'] and payload['mix_note'] are None in every payload this gate builds, so the strat=True/False axis is a no-op: base_payloads[('Pay',True)] and [('Pay',False)] are the same object shape, and the pay_pay/pay_abs distinction Section B relies on (lines 88-89, check 8) is fictional. Never exercised: the floor's strategy clause at claude_api.py:1639-1646 ("Your strategy for this  | confirmed | **yes** |
| 74 | SECTION A — 'shipped output passes validator' / 'source is model\|deterministic' | fixture-omits | The fixture omits the second positional argument and takes the default allow_ai=True. Production ALWAYS passes it: app.py:7379 `await to_thread.run_sync(claude_api.generate_domain_summary, payload, _ai_ok)`, where `_ai_ok = ai_feature_on(ai_gate(get_conn(), user), AI_DOMAIN_SUMMARY)` (app.py:7340) and AI_DOMAIN_SUMMARY defaults to off (app.py:157) — so the value production sends today is False for every member. | The `if not allow_ai:` early return (claude_api.py:1696-1698) — the branch that actually ships to every member while the compliance gate is off — is never executed by any check. The gate reaches the identical fallback through a different door (`_client_or_none() is None`, line 1699) after deleting ANTHROPIC_API_KEY at gate lines 36-37. So Section A proves 'the floor ships when there is no API key', not 'the floor shi | confirmed | no |
| 141 | SECTION B check 9 — 'market position on non-competitive payload rejected' (and S | both | The only has_position=False payload in the file is hand-mutated. Every payload build_domain_summary_payload actually returns in this run has has_position True (data/market_position_config.json marks all 8 _domains competitiveness:true, and the chosen org is submission_complete), and the floor fed to the check is built from `gov` — the has_position=True payload — so no code path ever runs the generator on a non-compet | _deterministic_domain_summary is never invoked with has_position False, so its non-competitive branch (claude_api.py:1656-1664 — "This domain has no market position to read; lumi assesses it on practice alignment and approach instead." plus the "%s are off the norm and %s are in line" approach counts) and the payload's `approach` block (app.py:7141-7142) are never generated, never validated, and never number-checked  | confirmed | **yes** |
| 69 | SECTION A/D — provenance line of the shipped summary | fixture-omits | With the org the fixture picks (industry 'Hospitality, Leisure & Travel') and the two cuts it sends, _cut_peer_n returns 270 (dim=all → meta.peer_pool.responding_orgs) and 33 (dim=industry) — both ≥ 20, so payload['small_sample'] is False in every payload this gate builds. Production routinely sends cuts under 20: twin cuts, custom groups, and thin sectors in the same meta.peer_pool map (Healthcare 9, Charity 10, Ene | The small-sample provenance branch (claude_api.py:1621-1622, tail " — a small peer group, so read this as directional") is never produced or validated; only the " in this group" tail is. The model prompt's rule 10 small-sample instruction is likewise never checked against a payload that sets the flag. Which branch runs is decided purely by which org sorts first by org_id — the gate has no assertion that pins it, so a | confirmed | **yes** |
| 53 | SECTION A — 'the LIVE generation path' (docstring claim that the harness attacks | fixture-invents | A hand-built two-key dict stands in for production's session user, which is the full identity row returned by require_user(request) (user_id, org_id, email, role, …). Inside the payload builder the only consumer is make_entitled (app.py:734-737), which ignores both arguments and returns `lambda q: True`, so nothing diverges there — but the fixture's dict has no user_id, so it could not be passed to ai_gate(conn, user | The gate cannot call the real route, and does not: there is no HTTP call to /api/domain-summary anywhere in the file. Everything the route does around the payload is therefore unexercised — the peer_groups lookup with its foreign/stale-group-id → {'dim':'all'} fallback (app.py:7352-7359), the fte_band/industry value defaulting (7346-7349), the 404 when the payload is None, _ai_generation_or_429, and the cut_key + pay | likely | **yes** |

## qa_engine_audit.py — 9 candidates

| line | check | direction | divergence | consequence | conf | prints |
|---|---|---|---|---|---|---|
| 466 | LAYER 2 — CALCULATION (independent recompute), all 333 Reward metrics | both | `dim` is not a query parameter the server reads. app.py:786-816 parse_cut reads `cut` and `cut_value` only (grep for query_params.get("dim") in app.py returns nothing), and the client builds exactly that pair: web/js/app.js:1604 `"cut=" + sel.dim + (sel.value ? "&cut_value=" + sel.value : "")`. The gate never sends `cut`, so every one of these ~333 requests falls to parse_cut's default {"dim":"all","value":None}. Pro | Layer 2 is the only place in this gate where production's SERVED numbers are compared against a fresh independent recompute, and it sees payload["all"] for every metric and nothing else. positions.resolve_block's by_industry / by_fte_band branches (positions.py:72-77) and block_for / score_block_for / presence_block_for / matrix_row_block_for's twin+group branch (positions.py:83-111) are never reached by any of the 3 | confirmed | **yes** |
| 698 | LAYER 3 — below-floor custom group end-to-end (suppression / isolation) | fixture-omits | This is the gate's ONLY bespoke-cut request, and the group is selected to be below the floor by construction (line 679-682: `HAVING c BETWEEN 1 AND FLOOR-1`). twin_blocks_if_needed therefore returns `{}` (app.py:824-829), which is falsy, so the route passes `twin_blocks_by_q=None` at app.py:2189 (`{qid: tb.get(qid)} if tb else None`). Production passes a POPULATED {question_id: {main, score, presence, rows}} dict on  | The twin/group branch of block_for is exercised only in its twin_blocks=None form, which returns None → suppressed. The blocks peer_twin computes live via aggregate_question_for_orgs are NEVER written to benchmark_snapshots, so they are also outside Layer 3's stored-payload leak scan (line 663 walks benchmark_snapshots only) and outside Layer 2's recompute. Result: for a member whose company default peer group is Pee | confirmed | **yes** |
| 852 | EDGE CASES — production numeric_block under crafted input | fixture-omits | `excluded` is omitted at all five call sites, so it defaults to 0. Both production call sites pass it positionally and always compute it: aggregate.py:702 `blk = numeric_block(vals, excl)` for matrix rows and aggregate.py:744 `blk = numeric_block(vals, excl)` for numeric metrics, where excl counts the answers matrix_value/coerce_number could not read. | The sub-floor branch at aggregate.py:109-113 — `if excluded: b["excluded_non_numeric"] = excluded` — is unreachable from this section, and `excluded_non_numeric` is 0 in every crafted block. The precise edge this section exists to probe (a block that is BOTH below the suppression floor AND carrying values the parser rejected — the shape that decides whether an unreadable answer vanishes silently or is counted) is the | confirmed | no |
| 853 | EDGE CASES — production numeric_block under crafted input (org identity / compos | fixture-invents | The org ids "qa-o0"…"qa-o5" exist in no table. Production only ever hands numeric_block ids drawn from the answers table (aggregate.py:739 `pairs = list(per_org.items())`, keyed by orgs.org_id). numeric_block feeds those ids to _nreal (aggregate.py:91-93), which intersects them with real_org_ids() = `SELECT org_id FROM orgs WHERE source NOT IN ('seed','staff','demo') AND submission_complete=1`. | n_real is 0 in every crafted block by construction — the intersection is empty whatever the DB contains, today and after the first real member joins (which is exactly when composition starts mattering). The n-vs-n_real split is therefore unexercisable in the EDGE section, and nothing else in the gate asserts n_real either: Layer 2 compares p10..p90, option counts and n only. n_real is what renders as compositionLabel | confirmed | **yes** |
| 710 | LAYER 3 — AI commentary on the suppressed cut must carry no peer numbers | fixture-omits | Production never reaches this endpoint without require_ai passing (app.py:726-731: AI master switch AND member consent AND the per-feature flag) and always sends one of `peek: true` on page load (web/js/app.js:1594-1595) or `force: <bool>` from the generate button (app.js:1728-1729). The gate sends neither, and run_gates.sh:84/99 starts every gate server with ANTHROPIC_API_KEY='' LUMI_AI_LIVE='' and never sets LUMI_A | The POST returns 403 and the gate never inspects `st` (line 710 binds it and discards it). The `\d+%` regex then runs over the FastAPI error JSON, finds nothing, and the check prints 'AI commentary on the suppressed cut: clean'. build_commentary_payload, claude_api.generate_metric_commentary, the post-generation validator and the peek branch are all unexecuted — so the only path that could put a peer number into memb | confirmed | **yes** |
| 742 | LAYER 3 — cross-tenant: a foreign org's raw data must be unreachable | fixture-invents | /api/my-data (app.py:2921-2940) never touches request.query_params. The org is `require_user(request)` → request.state.org, resolved from the session cookie at app.py:470. No client code appends org_id to this endpoint. The gate invents an injection parameter the server discards. | `returned` can only ever be the caller's own rows, so `not_ours` is empty by construction and the check is structurally incapable of failing regardless of what the org-scoping code does. The comment at lines 730-738 explicitly claims that after the name-canary was found inadequate the property is now 'asserted directly — every row returned must be one of the CALLER's own answers'; it is asserted against an input with | confirmed | **yes** |
| 807 | LAYER 4 — custom group set correctness + self-inclusion | both | Hardcoded single-field criteria with a literal sector; the only other criteria the gate builds is industry+fte_band (line 686). Production's builder posts criteria over all eight GROUP_FIELDS (peer_twin.py:133-134: industry, fte_band, hq_region, ownership_type, unionised_level, hr_maturity, business_maturity, operating_model) assembled from /api/peer-groups/options. The gate's own reference at lines 809-810 re-implem | No group this gate builds can contain an org matched through registry_json rather than an orgs column, so org_group_value's fallback and _union_band are never exercised end-to-end; nor is the exited-org exclusion. Group membership is the denominator behind every number on every card under a group cut, and the count assertion at line 812 only agrees today by accident of the seed data (verified read-only: 0 orgs with e | confirmed | **yes** |
| 827 | DETERMINISM — re-aggregating identical data must not change output | fixture-omits | The argument shape matches production exactly (app.py:7008 `await to_thread.run_sync(run_snapshot, CURRENT_SNAPSHOT, False)`), but production's only caller invokes it immediately after INSERTing the org's new answers and deleting its drafts (app.py:6988-6997), and always follows it with invalidate_payloads() (app.py:7009). The gate invokes it out-of-process against an untouched answers table, and cannot invalidate th | The probe proves idempotence on unchanged input and nothing else — a run_snapshot that wrote nothing at all (early return, wrong snapshot_id filter, a swallowed per-question exception) produces a byte-identical hash and passes DET. The state production always pairs with this call — answers changed, so payloads MUST move and the cache MUST be dropped or the next card serves stale numbers — is never exercised. The foll | confirmed | **yes** |
| 417 | cache-freshness self-check + every served-surface comparison (L1/L2/L3 API arms) | fixture-invents | Every API comparison in the gate runs as one hardcoded seed org — the demo org (identity.DEMO_ORG_NORMALIZED = 'thornbridgeretailgroupplc'), source='seed', submission_complete, unlocked, one fixed industry (_demo_ind at line 317). Production serves orgs in states this org can never occupy: the reduced-contribution state (app.py:2160-2168 returns a card with NO block, no real cut and reduced:true), pre-unlock, and eve | The reduced-contribution card shape — what a lapsed member sees on EVERY metric — is never produced, so nothing here would notice if it began shipping n/block/percentiles. The sector-scope canary (lines 317-329 and the by-design 404 acceptance at 472-474) can only ever prove the hide for the demo org's single industry: a scoped metric that leaked into any other sector is invisible to it, and the 404 acceptance branch | confirmed | **yes** |

## qa_focus.py — 10 candidates

| line | check | direction | divergence | consequence | conf | prints |
|---|---|---|---|---|---|---|
| 117 | board pack AI deferred — gate off pre-go-live (LUMI_AI_INSIGHTS_ENABLED); genera | fixture-omits | The fixture branches the ENTIRE board-pack block on me.features.boardpack and asserts a literal True when it is false, on the stated premise that "/api/boardpack/generate correctly 403s" with AI off. That premise is false against app.py. me.features.boardpack is ai_feature_on(_aig, AI_BOARDPACK) (app.py:1315) — the AI-narrative gate only. POST /api/boardpack/generate 403s solely on role (app.py:4622) and on insights_ | Ten checks — pack_id, hidden-area leak on the pack payload+narrative, the n>=5 suppression floor, unsuppressed gap_register_top rows, strategy_review presence, directive-free statements, alignment_counts reconciliation, suppressed-measure n, the ruled options framing string, and the 'Comparison pool:' provenance footer — are dead in every AI-off run, i.e. in the entire documented pre-go-live configuration the gate is | confirmed | **yes** |
| 58 | overview headline reward-scoped (comparable_metrics <= the Reward question unive | fixture-omits | No ?cut=/?cut_value= is sent, so parse_cut (app.py:786-816) yields {"dim":"all","value":None} and build_items (app.py:2290-2296) passes twin_blocks_by_q=None to position_items, prevalence_items, practice_position_items, money_opportunities, disclosed_absent and gap_register. Production lands every member of THIS org on ?cut=group&cut_value=76188216-8c5b-4cc2-93ba-5834872586fa (orgs.default_cut → me.org.signal_peer_cu | The requested-cut half of /api/overview — overview_summary (the only site that also passes absent_items, app.py:2343), disclosed_absent, pool_market_bands, hero_signals/domains, callouts and money — is only ever computed with twin_blocks_by_q=None. pos.block_for(p, cut, tb) is never called with a real block dict on any surface these checks assert on, so comparable_metrics, by_section, by_superpower and action_gaps ar | confirmed | **yes** |
| 165 | metric endpoint applies a sector cut (n differs from all-peers) | fixture-omits | The only cut-bearing probes in the file cover dim=all and dim=industry. Neither produces a twin block: twin_blocks_if_needed returns None for every dim except twin/group (app.py:831-832). So at app.py:2188 assemble_card and app.py:2194 money_opportunities always receive `None` from the expression `{qid: tb.get(qid)} if tb else None` — the ONE production site that builds a single-key twin-blocks dict whose value may i | The single-metric endpoint's twin/group path is never exercised by the check whose own comment says 'a regression here is what the user hit on the metric page'. Neither block_for's twin branch nor the bespoke one-entry-dict construction at 2188/2194 can be reached, so a TypeError or a wrong-block selection there — and the £ opportunity panel computed from the same one-entry dict — reads green forever. The check prove | confirmed | **yes** |
| 175 | shared view clean of hidden areas | both | The fixture stores config.cut as a DICT. The primary production writer, ShareDialog (pages.js:400-406), sends `config: {cut: (cut && cut.dim) \|\| "all", cut_value: (cut && cut.value) \|\| null, layout: [...]}` — a dim STRING, a separate cut_value, and a layout array; the dict form is sent only by the Settings quick-create (commercial.js:772). Because the fixture always sends a dict on an 'all' cut, app.py:8943 takes | The only leak check covering anonymous share links proves cleanliness for a payload shape the Share button never produces. Three production branches on the public path stay unexercised: the string→{dim,value} normalisation the code itself records as having 500'd before the fix (app.py:8942-8943); the rewrite that enforces 'bespoke groups never exposed on anonymous links' — the exact privacy rule this check exists to  | confirmed | **yes** |
| 131 | board pack honours the n>=5 suppression floor (no sub-floor figure exported) | fixture-omits | The body carries only `cut: "all"`. The dim-string shape is right, but cut_value is never sent and the dim is never anything but all. Production sends {cut: cut.dim, cut_value: cut.value} from the ACTIVE cut (pages.js:326; regenerate at commercial.js:189-190 replays the stored pack's cut), which for this org is the group default. app.py:4634-4649 therefore never resolves a saved peer group (the peer_groups lookup tha | assemble_pack_payload's position_items/practice_position_items/prevalence_items/money_opportunities/gap_register calls run only on all-peers, where n is at its maximum. The below-floor group contract (group_blocks → None → tb={} → every question suppressed) and the twin path are both unreachable, so a suppression-floor break can only be detected in the one configuration where it is least likely to occur. The same omi | confirmed | **yes** |
| 77 | gap register rows all Reward | fixture-omits | No cut is sent, so build_gap_register (app.py:3731-3737) runs with cut={"dim":"all"} and tb=None (its tb is guarded on dim in twin/group at 3734). The production caller always sends the active cut: commercial.js:30 `api("/api/gap-register?" + cutQS(cut))`, and the CSV links at commercial.js:80 and pages.js:2465 do the same — for this org that is the group default. | pos.gap_register is never exercised with a bespoke primary cut alongside its sector_cut second cut (app.py:3735), so the interaction that decides suppressed / gap / peer_adoption_pct / n_real on a group pool is unchecked. The register's rows and maturity — which print on the Priorities page, in the gap-register CSV, and as hero['action_gaps'] on the dashboard — are proven Reward-only and non-empty solely on all-peers | confirmed | **yes** |
| 73 | £ opportunity only reward metrics (pension; no agency/attrition) | fixture-omits | money_opportunities is reached at app.py:2364 with the requested cut and tb — both fixed at all/None by the missing cut params. Production hits it with the group cut and a populated tb at 2364, and with the single-key {qid: tb.get(qid)} dict at 2193 on the metric page. The engine also reads org['fte_band'] (positions.py:613-614); the fixture org has fte_band '50-249', so the fte_known=False fallback that un-profiled  | The assertion that every £ item is 'Employer pension contribution' is established only for the all-peers pool. A money item that qualifies only against a bespoke peer median (a different peer gap flips the qualification) never enters the assertion, so the 'no agency/attrition' leak claim does not hold for the pool a member actually sees. The £ figures print on the dashboard opportunity panel and in the board pack. | confirmed | **yes** |
| 57 | overview clean of hidden areas | fixture-omits | The fixture never sends ?strategy=off, so app.py:2331 always computes apply_strategy=True and _strategy is always the full strategy_for_engine dict (this org has org_strategy.completed_at set). Production sends the parameter whenever the 'Apply my strategy' toggle is off — pages.js:176, 2133, 2838, 3027, 3333 all append '&strategy=off' from the stored pref. | The strategy=None degrade path is never leak-checked: hero_signals(strategy=None) produces the legacy hero and legacy signal ordering, and build_signals(strategy=None, domain_alignment={}) drops confirm-suppression (app.py:2417-2419). Those are different domains, different signal text and a different by_section/target payload from the ones scanned — a hidden-domain string reachable only on the unlensed hero or the le | confirmed | **yes** |
| 53 | /api/benchmarks/Reward serves a card per visible Reward question | fixture-omits | No cut is sent, so the grid is assembled with tb=None and the per-card single-key dict at app.py:2128 is always None; production sends cutQS(cut) (pages.js:3028) on the group default. Compounding it, line 161 derives the cut-sensitivity probe as the max-n non-practice card FROM THIS all-peers response, so the probe is by construction the metric with the largest all-peers pool. | Every card in the only per-card leak/count check is built with twin_blocks_by_q=None, so pool_market_bands/pool_prevalence_bands and each card's market_band/prevalence_band chips are validated only on all-peers. The derived probe additionally biases the downstream 'n differs from all-peers' assertion toward passing: a widest-pool metric is the one most likely to survive a sector cut with a different n, so the check i | confirmed | **yes** |
| 107 | saved layout with hidden card degrades to reward-only | fixture-omits | The fixture's slots carry only question_id and size. Production slots carry row_id and a per-slot cut dict: starter_layout writes {"question_id", "row_id", "size", "cut": {"dim": "all"}} (app.py:3100-3101), _norm_layout explicitly normalises row_id and a per-slot cut via _norm_dash_cut (app.py:3255-3261), and the dashboard renders each slot with cutQS(slot.cut \|\| activeCut) (pages.js:3346). | The hidden-card degrade is proven only for the simplest slot shape. A matrix-row slot (row_id set) and a slot carrying a stored bespoke cut never round-trip through _norm_layout and keep() (app.py:3116-3117), so a normalisation that dropped row_id, or a degrade that mishandled a matrix row or a stored group cut, would not be caught here — and the assertion compares question_ids only, so it could not see it even if th | likely | no |

## qa_hero.py — 13 candidates

| line | check | direction | divergence | consequence | conf | prints |
|---|---|---|---|---|---|---|
| 237 | no answers -> market None, prevalence None, domain shows insufficient | fixture-omits | Omits all four kwargs production supplies at 9/9 sites (app.py:2347, 2411, 2560, 2810, 4291, 4431, 5418, 5908, 7072): practice_items=prac_items, tile_min=TILE_MIN_POSITIONED, mp_config=mp_cfg, strategy=. positions.py:1367 forks on `if mp_config and mp_config.get("metrics")`; data/market_position_config.json holds 340 metrics, so production ALWAYS takes the classified fork. Also passes domain_min=5 where production pa | Section 6 executes the legacy polarity branch at positions.py:1370-1402, which no member is ever served by. The engine says so itself twice: hero_signals' docstring ("keeps qa_hero's synthetic fixtures valid") and positions.py:1485 ("Classified path only — the legacy path serves qa_hero's synthetic fixtures"). Nothing in _hero_signals_classified is exercised on a day-one org: substance_pool routing, the _mp_gauge_eli | confirmed | **yes** |
| 240 | ...but it does earn a disclosed indicative tile position | both | question_id is the literal "x". Production items always carry a real library id present in data/market_position_config.json (340 metrics). On the production path the item first passes substance_pool (positions.py:1136) -> _mp_gauge_eligible (positions.py:1076-1094), which FAILS CLOSED for any question_id absent from a populated metrics map: `return (not cfg.get("metrics")) and item["polarity"] in (...)` — False here. | With mp_config passed as production passes it, this item is dropped from gauge_pool, d_sub is empty, and positions.py:1461-1466 returns position=None, basis=None — so the check asserts the exact opposite of production behaviour and passes only because mp_config was omitted. The TILE_MIN_POSITIONED indicative-position rule (app.py:124) — the "disclosed indicative tile" every thin-data member sees on their home domain  | confirmed | **yes** |
| 258 | practice position ranks presence against the peer block (Yes vs 70% adoption ->  | fixture-omits | Six positionals. Production passes seven at all 14 sites (app.py:2102, 2177, 2246, 2315, 2410, 2557, 2804, 4285, 4367, 4714, 5412, 5906, 7065, 8950); the 7th is twin_blocks_by_q — a real {qid: blocks} dict on twin, and on group a dict that can legitimately be {} meaning 'every question suppressed' (app.py:829). The fixture leaves it defaulted to None, and additionally pins the cut to {"dim": "all"} at all six _ppi in | positions.py:243 resolves the peer block via `block_for(p, cut, twin_blocks_by_q.get(qid))`, and block_for (positions.py:81-86) branches on `cut.get("dim") in ("twin","group")` to read `(twin_blocks or {}).get("main")`. With dim="all" and no tb, that branch is structurally unreachable — the same hole as the confirmed qa_overview.py:73 case. Practice positions on a Peer Twin or custom-group cut feed the gauge (as Prov | confirmed | **yes** |
| 247 | practice position ranks presence against the peer block (Yes vs 70% adoption ->  | fixture-invents | A hand-built stand-in for a library Question, with id "fq", and a hand-built payload block. positions._item (positions.py:291-292) does `if (market_position_config().get("metrics", {}).get(q.id) or {}).get("unbenchmarked"): pol = "neutral"`. The live config holds 340 metrics of which 24 are flagged unbenchmarked; "fq" matches none, so the lookup is always empty. The fixture block also lacks n_real, p50 and _values/_s | The Diff-14 unbenchmarked -> polarity=neutral suppression inside _item is unreachable from any practice-position check. That layer's own comment records it was added after "a 'behind' signal firing on suppressed PROP_168a6213" — i.e. it exists because the defect already shipped once. An unbenchmarked Provision metric leaking a practice position (into the gauge via substance_pool, into the tile rollup, and into signal | confirmed | **yes** |
| 265 | an N/A answer is never practice evidence | fixture-invents | "Not applicable" is not one of _FQ's option labels (only Yes/No, L250), and _FQ carries scoring_config={} so na_codes is empty and no option has is_na. aggregate.practice_status resolves the label FIRST: `o = by_label.get(_norm_label(raw)); if o is None: return "unknown"` (aggregate.py:596-598). Production N/A always arrives as a REAL stored option label whose option has is_na=True, or whose code is in scoring_config | The check exits down the unrecognised-label branch and never touches any of the three real N/A detectors. If a regression stopped honouring is_na, na_codes, or the _NA_LABEL regex, a "Don't know" / "Prefer not to say" / "Not applicable" answer would be scored through STATUS_POINTS into a ranked practice position — appearing as real evidence on the home domain tile, in position_evidence, and in the Provision half of t | confirmed | **yes** |
| 373 | briefing cap fallback yields 5 when 5 exist across lenses (never a blank slot) | both | Production (signals.py:1278-1281) calls cap_briefing(pool, cfg.get("max_signals",5), cfg.get("max_per_lens",2), cap_cfg.get("max_behind",3)) — and the LIVE values are max_signals=3 (data/signal_lenses.json) and max_behind=2 (ordered_scale_routing.json _david_ratified_2026_06_13.briefing_cap). So production is (3,2,2) with reserve_nb=1; the fixture invents (5,2,3) with reserve_nb=2. The fixture's _mk dicts (L371-372)  | Under the ratified live knobs this exact 4-behind + 1-outlier pool fills in pass 1 and returns 3 — the pass-2 fallback the check is named for is never reached with the numbers members actually get, so the 'never a blank slot' guarantee is asserted only at a cap production does not use (the same file reads the real cfg["max_signals"]=3 at L294 and L301). And because no fixture signal sets confirm=True, the STEP-3 LAYE | confirmed | **yes** |
| 30 | strength/gap items: zero favourable<->market contradictions | fixture-invents | Production passes MARKET_BAND_LOW/MARKET_BAND_HIGH = 35.0/65.0 at every site (app.py:108-109, LUMI_MARKET_BAND default "35-65"). The gate's own L130 check asserts the shipped band IS 35/65 while every classifier call in the file uses 25/75 — the file contradicts itself about the band it is testing. | Two 10-point strips of polarity-adjusted percentile — [25,35) and (65,75] — are classed "at" by every gate call but "below"/"above" by production. Real items re-classified at L68 that land in a shoulder are examined under a verdict no member sees. app.py:105-107 records that this band is deliberately kept in sync with cardPosition (card colour) and thresholds.behind_percentile so the card colour, the tile verdict and | confirmed | **yes** |
| 35 | higher_is_better, you mid (P50) -> at market (and the other five polarity-direct | fixture-omits | The synthetic item carries exactly two keys. Every production item from positions._item carries tie_half (positions.py:303-314), and practice items set theirs explicitly at positions.py:267. _market_class reads it FIRST, before any band comparison: `th = item.get("tie_half") or 0; if th and abs(50.0 - a) < th: return "at"` (positions.py:947-951). | The median-tie => at-market override — David-ruled 2026-08-12, generalising the RED_TERM_01 tie-at-top ruling, and mirrored client-side at web/js/card.js:423-424 — is unreachable in all six cases of Section 1. That rule can convert a genuine below/above into "at" on a card, on a domain tile, and in the gauge's below/on/above counts. An inversion or sign error in it (e.g. `>` for `<`, or the halfwidth not halved) woul | confirmed | **yes** |
| 227 | verdict re-derives from the same counts | both | Production only ever calls _pool_verdict on substance_pool output (app.py:4386, 4718) or on the in-engine gauge_pool (positions.py:1461, 1525) — entries carrying question_id/row_id/label/subpower/distance/n/n_real/tie_half. The fixture rebuilds a pool of two-key dicts at percentiles 80/50/10 chosen to sit far outside any band, passes bands 25/75 while the value it compares to (m["verdict"]) was produced at 35/65, and | The re-derivation cannot disagree with the API by construction: it reuses the API's own three counts and picks percentiles that classify identically under any band in [11,79]. It validates the lean arithmetic only — never that the counts came from the right pool, and never the tie_half branch of _market_class that every real pool member triggers. In the other direction it is fragile rather than blind: because the mar | confirmed | **yes** |
| 162 | a tighter band (40-60) reclassifies the same pool differently (tunable) | fixture-invents | Bands 40/60 appear nowhere in production — MARKET_BAND_LOW/HIGH are 35.0/65.0 at every call site. The six pool entries are again two-key dicts with no tie_half and none of the substance_pool keys production entries carry. | A tunability probe that establishes nothing about the band members are actually served, and whose hand-picked percentiles (30/35/40/60/65/70) sit exactly in the region where a tie block would flip the classification — so the tie override that would change these very results is bypassed here too. Low independent value; listed for completeness alongside the L227 case. | likely | no |
| 61 | strength/gap items: zero favourable<->market contradictions (and every HTTP-fed  | fixture-omits | All six HTTP calls omit the `cut` query parameter, so app.parse_cut (app.py:786-791) returns {"dim":"all","value":None} on every request, and build_items (app.py:2294) therefore never calls twin_blocks_if_needed — tb is None into position_items, prevalence_items, practice_position_items, money_opportunities and gap_register. Production reaches all five dims (all\|industry\|fte_band\|twin\|group). The org is also a si | The entire twin/group block-resolution family — block_for, score_block_for, presence_block_for, matrix_row_block_for (positions.py:81-111) — is unreachable from every check in this gate, the same class as the confirmed qa_overview.py:73 hole and the class in which P0-7f actually shipped. Also never touched: the group-below-suppression-floor contract (twin_blocks_if_needed returning {} so every metric renders suppress | confirmed | **yes** |
| 316 | no signal contradicts its metric (no 'behind' on neutral; all kinds from David's | fixture-invents | The signals under test were built at sig_cut = _org_sweep_cut(conn, org) — the org's DEFAULT cut (app.py:2399, which can resolve to industry::X, fte_band::Y or group::gid, app.py:2654-2672) — while the card the gate reads back to judge each signal's polarity and direction is pinned to ?cut=all. The two are the same object only when the org default IS all-peers; app.py:2397 notes the alias is a no-op "when the request | When the org's default cut is not all-peers, the gate judges each signal against a different peer basis than the one that fired it: the polarity, suppression and unbenchmarked state read from the all-peers card need not match the block the signal actually used. And the whole re-point branch (app.py:2402-2418 — its own build_items, money_opportunities, prevalence_items, practice_position_items and a SECOND hero_signal | likely | **yes** |
| 75 | every lower_is_better Reward metric is ruling-accounted (ruled ↓ set empty since | fixture-invents | library.load_questions (library.py:103-107) returns every row of `questions` — including status='retired' and superpowers outside ACTIVE_SUPERPOWERS. Production never hands the engine that set: every engine site receives org_visible_questions(org) (app.py:365-385 = visible_questions() minus retired and non-active superpowers, minus module metrics for non-tronc sectors, minus sector_scopes-excluded metrics), or visibl | A superset, so the two polarity checks at L88 and L117 are conservative rather than blind — but the coverage census printed at L100-101 ("N polarised -> M positionable, K routed to prevalence") describes a pool no member's engine ever receives, and the sector_scopes / module narrowing that decides what a hospitality org sees versus everyone else is not exercised anywhere in this gate. The failure mode is inverted fro | confirmed | no |

## qa_overview.py — 11 candidates

| line | check | direction | divergence | consequence | conf | prints |
|---|---|---|---|---|---|---|
| 72 | 7. every shown (non-suppressed) metric block has n >= SUPPRESSION_FLOOR (no unde | fixture-omits | twin_blocks is hard-coded None and cut is pinned to {"dim":"all"}. Production's equivalent lambda is app.py:2379 — pos.block_for(payloads().get(qid) or {}, cut, (tb or {}).get(qid))[0] — where tb is a populated per-question dict on twin/group cuts, and the signal build re-points to sig_get_block (app.py:2419) over the org's DEFAULT group cut. For this gate's own default org (Thornbridge, default_cut=group::76188216…, | block_for's twin/group branch (positions.py:83-86, `b = (twin_blocks or {}).get("main")`) is unreachable from this file — the same branch where the P0-7f TypeError lived. Check 7 iterates all ~333 visible questions through this lambda to assert n >= 5, but the only pool it can ever inspect is the 271-org all-peers pool where n is structurally large. The group pool — 44 orgs, where a sparsely-answered metric genuinely | confirmed | **yes** |
| 73 | 3. distinct differs-position signals <= hero differ / 8. zero non-competitive-do | fixture-omits | domain_alignment= is OMITTED entirely. Production passes it at every overview-path site: app.py:2417 and 2426 pass domain_alignment=sig_align, app.py:2816 passes dom_align, app.py:5957 passes _sig_align. The map is built from the hero's per-domain targets, and the gate's own default org has six domain_targets on a COMPLETED strategy, so sig_align is non-empty for exactly this org. | signals.py:1182 sets s["alignment"] = (domain_alignment or {}).get(s["domain"]) — with the kwarg omitted every signal in the gate's set carries alignment=None. That kills the whole confirm-suppression layer (signals.py:1183-1188: s["confirm"]=True plus the CONFIRM_DEMOTE_MULT impact demotion) and the cap-aware confirm shedding in cap_briefing. Checks 3 and 8 scan a signal set that is structurally incapable of carryin | confirmed | **yes** |
| 73 | 9a. engine emits hero prevalence + signals-total as integers (the 'See all N' se | fixture-omits | statuses={} is a literal empty dict. Production reads the real per-user triage state first (app.py:2382-2389: SELECT question_id, status, snooze_until FROM signal_actions WHERE org_id=? AND user_id=?) and passes it as statuses=_statuses. For the gate's default org that table holds 23 'dismissed', 6 'saved' and 1 'snoozed' row for its user. | signals.py:935 (`st = statuses or {}`) then :950 (`s["status"] = st.get(s["sig_id"])`) leaves every signal's status None. Check 9a computes signals_total_val = len([s for s in sigs_all if s.get("status") != "dismissed"]) — with an empty statuses dict that filter is provably a no-op, so the gate's 'See all N' total is just len(sigs_all) and can never diverge. On the live page the same total is computed with 23 dismiss | confirmed | **yes** |
| 73 | 3 / 8 / 9a (the whole signals half of the gate) | fixture-omits | The gate builds only the cap=False set. Production's /api/overview builds BOTH in the same request: app.py:2417 calls build_signals with cap left at its default True (the balanced briefing, shipped as `signals` + `signals_practice`) and only app.py:2426 passes cap=False (shipped as `signals_all`). | Everything below signals.py:1255 (`if not cap: … return out`) is dead in this gate: the dismissed/snoozed exclusion at :1259, the priority impact boost at :1261-1263, the market/practice pool split at :1284-1285 and both cap_briefing calls with their ratified max_behind reserve, per-lens cap and confirm shedding. That capped set is the one the home panel actually renders — the gate's three signal checks (3, 8, 9a) on | confirmed | **yes** |
| 55 | 1a/1b/1c reconciliation, 2a/2b differ traceability, 5a depth, 12a drivers | fixture-omits | cut is the literal {"dim":"all","value":None} declared at line 47 and twin_blocks_by_q is None at all three sites. Production builds these three through build_items/parse_cut with a live cut and tb=twin_blocks_if_needed(...) (app.py:2294-2296, 2305, 2313), and the member's landing cut is the org's company default — for this gate's own org, a group cut over 44 peers with a full twin_blocks dict. | position_items' tb path (positions.py:152-157) and the group branches of block_for / score_block_for / presence_block_for / matrix_row_block_for are never entered, so the entire item set the gate reconciles is built from precomputed all-peers blocks. Every downstream assertion — hero below/at/above vs card sums (1a/1b), the competitive-scope differ denominator (2a/2b), depth_pctl (5a), the driver ordering (12a) — is  | confirmed | **yes** |
| 71 | (feeds signals 3/8 and the £ figures on the page) | fixture-omits | twin_blocks_by_q=None plus the all-peers cut; production passes tb at app.py:2364 and, on the signal path, recomputes sig_money against the org's default group cut with sig_tb (app.py:2413). Secondary: `org` here is a sqlite3.Row selected with an extra AND classified=1, where production always passes the dict from SELECT * (app.py:470). | matrix_row_block_for's twin/group branch (positions.py:107-111) never runs inside the £ model, so every gap-to-median figure the gate feeds into build_signals is computed against all-peers p50/p75 — never the group medians the member's page and the money signals actually print. The 'X below the peer median' £ strings (signals.py:543-552) are therefore exercised only in a shape production does not produce for this org | confirmed | **yes** |
| 312 | 11c. multi-select items stay disjoint from the market pool (the partition ruling | fixture-omits | Called directly with cut=all and twin_blocks_by_q=None. Production never calls this function from app.py at all — its only production reach is positions.py:858, inside prevalence_items, where the caller's real cut and twin_blocks_by_q are threaded straight through. | market_pool_qids re-derives position_items + practice_position_items + substance_pool internally (positions.py:820-823), so with tb=None the partition authority is computed over all-peers blocks only. The one-category-per-metric invariant is asserted for a membership set that cannot change under suppression; on a group cut a metric whose group block falls under the floor drops out of the substance pool and becomes el | confirmed | **yes** |
| 300 | 11b. every multi-select band re-derives from its own payload block | fixture-omits | Third argument hard-coded None again, inside the multi-select re-derivation loop; production resolves the same block through app.py:2379 / 2419 with (tb or {}).get(qid) on twin/group cuts. | The core-option set (_opts / MS_CORE_PCT) is recomputed from the all-peers option percentages. Whether an option counts as 'core' at the 50% threshold is a property of the peer pool, so on the group cut the core set — and therefore each metric's match / common_alt / rarer band — can differ. The gate's re-derivation proves the classifier only against the payload['all'] options, never against a group block, while the c | confirmed | **yes** |
| 53 | 2a/2b differ traceability, 3 signals subset, 12a drivers (everything strategy-le | both | The gate takes strategy_for_engine's raw return. Production never does on this route: app.py:2324-2328 nulls it unless org_strategy.completed_at IS NOT NULL, and app.py:2331-2334 nulls it again when ?strategy=off. strategy_for_engine itself (app.py:6461-6472) does no completed_at check — I read both sides. | Two paths go untested at once. fixture-invents: with a half-saved strategy row (completed_at NULL) the gate would lens hero + signals with a strategy every production surface treats as absent, so checks 2a/2b/3/12a would be asserting against a hero shape no member can see. fixture-omits: strategy=None — the §5.5 legacy degrade that ?strategy=off exists to serve (absolute RAG, no aim/target, impact-ordered signals, em | confirmed | **yes** |
| 41 | gate-wide org fixture (every check) | fixture-invents | A single hardcoded, maximally-provisioned org: I verified 5e67fa8c… has classified=1, submission_complete=1, insights_unlocked_at set, industry='Retail & Consumer Goods', fte_band='50-249', a completed strategy and a company-default peer group. Production serves the whole orgs table (271 rows), including orgs with NULL industry / NULL fte_band and orgs with no answers yet. | positions.py:614 (`fte = a["fte_band_midpoints"].get(org["fte_band"] or "", None)`) can never return None here, so the fte_known=False fallback — the path that decides whether a £ figure is shown at all or replaced by the honest 'we don't know your size' state — is unexercised. Likewise the empty-pool paths: check 5a asserts `mkt.get("depth_pctl") is not None and exp_depth is not None`, which is only satisfiable beca | confirmed | **yes** |
| 64 | 1a/1b. hero below+on+above == pool, and cards sum to the hero | fixture-omits | The gate reconciles the hero only. Production computes, in the SAME request and from the SAME items, a second denominator the gate never builds: _positioned_qids = set(pos.pool_market_bands(items, prac_items, mp_cfg, …).keys()) (app.py:2338), then pos.disclosed_absent(org_id, cut, …, mp_cfg, _positioned_qids, tb) (app.py:2340), then pos.overview_summary(items, …, absent_items=_absent) (app.py:2343) — the only site in | The Diff-16 N/A-disclosure path (disclosed_absent + overview_summary's absent_items branch) is never called from this file, so the headline numbers a member reads at the top of the Overview are reconciled against nothing. Check 1b's whole purpose is 'the hero reads what the cards render' — but the third number on that page, the disclosed-absent count and the denominator it adjusts, sits outside the reconciliation. A  | likely | **yes** |

## qa_refresh.py — 9 candidates

| line | check | direction | divergence | consequence | conf | prints |
|---|---|---|---|---|---|---|
| 350 | C10 re-confirming the same value clears the flag | fixture-omits | The round-trip subject is filtered to questions with EXACTLY ONE answers row and a null `rows` list, i.e. non-matrix only. Production submit (app.py:6982-6996) stamps submitted_at=datetime('now') ONLY for rows present in the drafts table, and the client PUTs one draft per edited cell (web/js/submission.js:191, matrix_row_id: rowId). Production therefore routinely submits partial matrix drafts; the fixture never submi | The end-to-end 'refresh clears on submit' path is never exercised for a matrix question. Because refresh_state's due-ness is MIN(submitted_at) over ALL rows (refresh_policy.py:71-74), re-saving some rows of a matrix leaves the question still due — the exact semantics B3a/B3b assert against an in-memory scratch table are never connected to what submit actually writes. 22 matrix questions are active and the gate's own  | confirmed | **yes** |
| 255 | C0b org is submittable once known obstructions are neutralised | fixture-invents | The fixture deletes every stored answer that fails current validation (the PAYCOMMS type-drift rows, ruling still pending per the gate's own comment at L246-248). Production never deletes them: they stay in the answers book, they keep ageing, and validate_all reports them as `problems` on every call because it validates the merged answers∪drafts set, not just the drafts being submitted (app.py:6936-6950). | The gate erases the one production state in which the refresh nudge is undischargeable: submit raises HTTPException(400) whenever validation['problems'] is non-empty (app.py:6963-6964), so an org holding a type-drifted stored answer cannot submit ANYTHING — every flagged metric keeps its 'Refresh' chip forever no matter what the member re-confirms. By neutralising those rows before the baseline, no check in the file  | confirmed | **yes** |
| 320 | C8 section endpoint agrees with data-overview on every flag | fixture-omits | The two endpoints do NOT compute the same field. /api/data-overview returns needs_refresh = bool(ans) and bool(rs.get('due')) where `ans` is the DRAFT-INCLUSIVE merge of answers and drafts (app.py:2995-3004, 3028); /api/submission/section returns the RAW engine value, bool((rstate.get(qid) or {}).get('due')), with no answered guard and no draft merge (app.py:6626). They coincide only when the org has zero drafts — wh | The only input state where the two endpoints can disagree — a pending draft over a stale committed answer, which is the normal autosaved state of a member mid-edit — is structurally excluded, so the agreement claim is unfalsifiable. On that state the Your-data list drops the Refresh chip (merged.pop clears the cell, ans falsy) while the section editor still ships needs_refresh=true; today only client-side patching hi | confirmed | **yes** |
| 306 | C5 unanswered questions are never due | fixture-omits | Production's `answered` is draft-inclusive (app.py:2995-3010) while rstate is answers-only, so `answered` and 'has an answers row' are genuinely different sets. The fixture deletes all drafts at L250, which collapses them into one set: every question with answered=False is also absent from rstate, so needs_refresh is False by construction regardless of the guard. | The `bool(ans) and` guard at app.py:3028 — the only thing that suppresses a refresh flag on a committed-but-blanked cell (member clears a stale answer, blank draft pops the key from `merged`, answered goes False while rstate still says due) — is never exercised. Remove the guard and this check still passes. | confirmed | **yes** |
| 302 | C3 top-level to_refresh == sum of domains == flag count | fixture-omits | refresh_state has TWO production consumers, and the gate calls only one. app.py:2499-2501 (/api/overview) computes loop.to_refresh = sum(1 for v in refresh_state(conn, org_id, CURRENT_SNAPSHOT, org_visible_questions(org)).values() if v.get('due')) — raw `due`, no bool(ans) guard, no draft merge — whereas the reconciled number this check verifies comes from data-overview's guarded formula. No other gate references to_ | The Overview loop counter has zero gate coverage in the file that owns the refresh feature, and its formula can legitimately disagree with the Your-data count it is meant to mirror: with a pending blank draft over a due answer, data-overview counts N-1 and /api/overview counts N. That number prints as 'Refresh N metrics' in the loop strip's next-step CTA and flips the data and plan pills to 'stale' (web/js/pages.js:4 | confirmed | **yes** |
| 331 | C9 viewer sees the same flags | fixture-invents | The viewer's response is compared to `due_expect` — a hand-built fixture literal containing only the three questions the gate backdated — not to the admin's actual response. Production returns pristine_due \| due_expect, which is exactly what the admin-side check asserts twelve lines earlier (C2, L299-300: want = pristine_due \| due_expect). | Role parity is never actually compared between roles. A viewer payload that DROPS the pristine-due flags (returns only the fixture's three) satisfies this check while the admin's C2 shows more — the precise regression 'viewer sees the same flags' names. In the opposite regime the same literal makes the check fail on a correct server as soon as any seed answer ages into pristine_due, defeating the clock-safe-by-design | confirmed | **yes** |
| 38 | C2 flags are exactly pristine-due + the fixture (controls clean) | fixture-omits | The single hardcoded org has industry='Retail & Consumer Goods'. data/sector_scopes.json scopes all five tronc/tips metrics to 'Hospitality, Leisure & Travel' only, so org_visible_questions(org) hands refresh_state 328 of the 333 active questions, and the org holds zero answers for the five excluded ids. Production also serves hospitality orgs (where those five are visible, answerable and flaggable) and fresh orgs (w | Section A asserts register coverage over the full 333-question bank, but the flag-producing path is only ever exercised over one retail org's 328-question visible slice: no check ever produces a refresh payload for a sector-scoped/module metric, and the empty-org shape (rstate {} -> refresh_months null, last_updated null, to_refresh 0 on every domain) is never rendered. A regression confined to either shape reads gre | confirmed | **yes** |
| 193 | B7a fallback: unregistered metric-category question -> 12 | fixture-invents | The three B7 checks pass synthetic ids (GHOST_Q1/2/3) that exist in no bank, so months_for misses the register lookup and falls through to the class rules. Production only ever passes real Question objects out of org_visible_questions(), and this gate's own A1 asserts the register covers EXACTLY the active bank (verified: 333 register entries, 333 active questions), so reg['months'].get(q.id) hits for every question  | B7 is the only coverage of months_for's class-rule fallback, and by A1's own invariant that branch is unreachable for every live question — it can only fire in a bank state where A1 has already failed. Meanwhile the branch production always takes (register hit) is covered only incidentally, by B1/B2/B8a. Note B7c is also self-limiting: because it passes category='benefit', a regression re-adding 'weeks' to _QUANT_UNI | confirmed | no |
| 181 | B5a provenance-tagged OLD timestamp reads as due | fixture-invents | The fixture inserts submitted_at values of the shape 'YYYY-MM-DD <tag>'. In the live bank zero of the answers rows have length != 19 (checked across all orgs), the column is `submitted_at TEXT NOT NULL DEFAULT (datetime('now'))` (server/db.py:135) and submit writes datetime('now') (app.py:6993-6995). The tagged shape came from migration STAMPs (e.g. migrate_diff15_underpeaking.py:28 = '2026-07-18 diff15', migrate_r3s | B5a/B5b exercise a value shape no live row holds, so they cannot fail on any real data; and nothing in the gate asserts the bank-wide canonical-timestamp invariant that makes the string comparison `last <= cutoff` safe (C12 checks only the single row the round-trip just wrote). Re-running any of the dozen migrations that still hardcode a tagged STAMP would silently reintroduce values that sort AFTER a same-date 19-ch | likely | no |

## qa_release.py — 12 candidates

| line | check | direction | divergence | consequence | conf | prints |
|---|---|---|---|---|---|---|
| 174 | accepted with both; logged in the emergency lane | fixture-omits | The 5th parameter `comparability_break` is never passed at either call site (170 or 174), so it defaults to False. Production /api/governance/emergency (app.py:1815-1818) ALWAYS passes it as the 5th positional: `bool(body.get("comparability_break"))` — it is a checkbox on the admin governance form, not an optional arg. | releases.py:239-240 — `if comparability_break: mark_comparability_break(qid, "%s+emergency" % (cur["release_id"] if cur else "live"), conn)` — is never executed by any gate, nor is the changelog detail's " Comparability break marked." suffix (releases.py:247). The emergency lane's ONLY member-visible output is that marker; the gate tests only the lane's refusal gate and its changelog row. A defect in line 240 (wrong  | confirmed | **yes** |
| 163 | trend splits between the two periods (no continuous line) | both | The check hand-rolls the trend splitter instead of calling it. Production (app.py:1841-1844) resolves every release position through `rel_pos(rid)`, which strips "+emergency" from the id and adds 0.5 to the base index; the fixture uses a bare `rel_order.get(b, -1)` on the raw marker string. It also compares snapshot 1 directly against the injected snapshot 999, whereas production (app.py:1874-1882) compares CONSECUTI | A marker of the form `break@<release>+emergency` — the ONLY form releases.py:240 ever writes — resolves to -1 in the fixture's arithmetic and can never satisfy `p1 < pos <= p2`. The emergency-break split is therefore structurally unreachable from the one check named for enforcing splits. The 0.5 mid-release offset (which is what makes an emergency break land between two release-stamped periods) is never exercised at  | confirmed | **yes** |
| 115 | break marker stored and parsed | fixture-invents | The fixture passes a BARE release id ("qa-release-fixture") as `release_id`. `mark_comparability_break` has exactly one production caller — releases.py:240, inside `emergency_change` — and it ALWAYS passes the suffixed form `"%s+emergency" % release_id`. No production path ever calls it with a plain release id. | The only marker the gate ever writes (`break@qa-release-fixture`) and the only one it round-trips through `comparability_breaks` → the split arithmetic is a string shape production never produces. The `+emergency` form is written by production, consumed by app.py:1842 `rel_pos` (base = rid.replace("+emergency","")) and by this gate's own hygiene check at line 59, but is never once produced or parsed by the trend-enfo | confirmed | **yes** |
| 146 | stamped org at %.0f%% completion STAYS unlocked after the release | fixture-invents | The fixture hand-INSERTs `insights_unlocked_at=datetime('now')` and `unlocked_release=prev_current` directly into orgs (lines 138-141). Those two columns are written ONLY by `org_unlocked` itself in production (app.py:246-251), with `unlocked_release = releases.current_release(conn)["release_id"]` computed at the moment of unlock. Pre-stamping means `org_unlocked` returns at app.py:241-242 and never reaches lines 243 | The engine-owned unlock-stamp path is never executed: `completion_pct` is not consulted, `submission_complete` is not consulted, and `releases.current_release(conn)` is never called to resolve the release the org unlocked under. A regression that stamped the WRONG release (e.g. reading the just-superseded row, or None because create_release momentarily leaves no status='current') would write a wrong/NULL `unlocked_re | confirmed | **yes** |
| 144 | stamped org at %.0f%% completion STAYS unlocked after the release | fixture-omits | 'qa-sticky-org' is INSERTed fresh at line 138 with no rows in `answers` and none in `drafts`. `completion_counts` (app.py:4807-4813) therefore returns (0, N) and comp is 0.0 against TARGET_PCT = 90.0 (COMPLETION_THRESHOLD 0.90, app.py:199). Production always calls this for an org with a real answer history; the sticky-unlock scenario the section is named for REQUIRES an org whose completion was >= 90% before the rele | The completion transition the check claims to protect is never constructed. The fixture org reads 0% before the release and 0% after, so `comp < TARGET_PCT` is tautologically true and the check cannot distinguish "the release added required questions and the org stayed unlocked despite dropping below target" from "the org was never eligible in the first place". The denominator-growth path — the only mechanism by whic | confirmed | **yes** |
| 126 | historical payload still resolves for the retired question | fixture-invents | A raw SELECT stands in for the production resolver. Every production path that reaches a benchmark payload first resolves the metric through a question map: /api/trend does `org_visible_questions(org).get(qid)` and 404s on None (app.py:1833-1835); `positions.position_items` / `prevalence_items` iterate `questions.items()` (positions.py:146), where `questions` is always `org_visible_questions(org)` or `visible_questio | The check asserts a row EXISTS, not that anything resolves it. In fact no production resolver can reach it: the question was just set to status='retired' by line 114, so `visible_questions()` drops it, `org_visible_questions()` drops it, /api/trend 404s on it, and `position_items` skips it entirely. The gate therefore certifies the module docstring's central promise ("historical benchmarks that used them still resolv | confirmed | **yes** |
| 128 | prior release reconstruction unchanged by the new release | fixture-invents | `question_set` is called with the hard-coded literal `rel.BASELINE_ID` ('2025-baseline') at both fixture sites (44 and 128). The single production caller, `create_release` at releases.py:172, passes `question_set(prev["release_id"], conn)` where `prev = current_release(conn)` — always the CURRENT release. Live DB: 2025-baseline holds 180 rows, 2026.1 holds 194, and the current release 2026.2 holds 206; the live Rewar | The reconstruction production actually consumes — 2026.2's 206-question set, the `prev_set` the release diff at releases.py:176-198 is computed against — is never reconstructed or size-checked by this gate; only the 180-row set three cuts back is. Worse, the CLEANUP at lines 189-194 restores `question_version`/`is_required`/`status` for flip[0..2] and RETIRE "exactly from the baseline snapshot (source of truth)" — i. | likely | **yes** |
| 132 | retired question gone from the live core | fixture-omits | The gate resolves the live core through the platform-wide `visible_questions()`. Every member-facing route resolves through `org_visible_questions(org)` (app.py:365-385), which applies two further filters on top: the tronc/hospitality module filter (`if ind not in TRONC_SECTORS: drop q.module`) and the data/sector_scopes.json per-metric sector filter. The same substitution recurs at line 156, `q = appmod.visible_ques | No org is ever passed, so neither filter layer at app.py:379-384 is in any gate path — the 5 sector-scoped tronc metrics and the whole hospitality module are invisible to this gate's notion of "the live core". And `flip` is selected by `is_required=0` (line 108), which is exactly the population module questions live in (module implies never-required, the invariant asserted at line 76), so the gate can select and mark | likely | no |
| 175 | accepted with both; logged in the emergency lane | fixture-omits | Production's /api/governance/emergency ALWAYS follows `releases.emergency_change(...)` with `load_questions.cache_clear()` then `invalidate_payloads()` (app.py:1821-1822). The fixture calls neither after line 174 — no cache_clear at all, and `invalidate_payloads()` is never called anywhere in the file (only `load_questions.cache_clear()` at 131 and 206). | The gate asserts only that a changelog row landed; it never re-reads the library, so it cannot see whether the new `question_version` written at releases.py:238 actually reaches `visible_questions()` / the member's metric-page caption (pages.js:4241 renders `question ${t.question_version}`). And the payload-cache invalidation half of production's post-change pair — `_payload_cache` reset plus `peer_twin.invalidate_tw | confirmed | no |
| 170 | refused without trigger AND without sign-off | fixture-omits | Both refusal cases pass a REAL, live question id (flip[0]). Production passes `body.get("question_id")` straight from the admin console JSON body (app.py:1816) — unvalidated, and None whenever the field is absent. | `emergency_change` has THREE refusals, not two: the missing-trigger raise (releases.py:229-231), the missing-sign-off raise (232-233), and `if r is None: raise ValueError("unknown question %s")` (234-236). The fixture can only ever reach the first two, and its assertion is literally `refused == 2`. The unknown/None question-id refusal — the one a real admin hits by mistyping an id or omitting the field, and the only  | confirmed | no |
| 180 | backlog add leaves the live core untouched | both | Called once, with a non-None `source_ref` literal, and the return value is discarded. Production has two shapes: app.py:1796 passes `body.get("source_ref")`, which is None for a hand-typed admin backlog item (the field is optional in the form); and app.py:7699 passes `"sugg-%d" % sid` and CONSUMES the boolean return as `promoted` — it ships as `promoted_to_backlog` in the response and in the `_audit` record. | Two branches of `add_backlog` go unexercised. The None-source_ref path (releases.py:254 `if source_ref and ...` short-circuits to a straight INSERT with no dedupe) is the admin console's default path and is never taken. And because the fixture calls it exactly once with a fresh ref, the dedupe hit — `return False` at line 256 — never fires, so the return value the suggestion-promotion endpoint reports to the reviewer | confirmed | no |
| 114 | release diff logs reworded + retired | fixture-omits | `replaced_by` is omitted, so it defaults to None and the fixture writes `replaced_by=NULL`. Production retirements-with-replacement DO carry it: migrate_r3sw8_pmicomposition.py:141 sets `status='retired', release_retired=?, replaced_by=?` to the successor metric id before the release is cut, and asserts it at line 176. | `create_release` reads it back at releases.py:196-198 and appends " (replaced by %s)" to the retired changelog detail when it is set. With the fixture always retiring an unreplaced question, that branch never runs — so the changelog line an admin reads to learn WHICH metric superseded a retired one is produced by no gate. `check("release diff logs reworded + retired")` only asserts the change_type strings are present | confirmed | no |

## qa_signals_system.py — 7 candidates

| line | check | direction | divergence | consequence | conf | prints |
|---|---|---|---|---|---|---|
| 163 | no verdict word in ANY fired signal, across all 8 classes (+ 'every fired signal | both | The fixture audits only the two CAPPED briefing keys. signals/signals_practice are slices of _sigs_brief = build_signals(..., cap=True), and the capped path filters the population first: signals.py:1262 `pool = [s for s in out if s["status"] not in ("dismissed","snoozed")]`. Production ALSO ships `signals_all` = build_signals(..., cap=False) (app.py:2427), which applies no status filter and is the set the Signals pag | 24 of the org's metrics are removed by signals.py:1262 before a single check sees them — including REW_BEN_PENS_EMP_MAX_01 (the one metric ALLOWED_OVERLAP is written for), REW_PAY_002 and PROP_8e0b6316 (both ruled ordered-outlier metrics), and 4 of the matrix-row sig_ids the dup/kind checks exist for (REW_Q528801::weekend, REW_Q534581::early_morning, REW_Q534581::weekend, a7ed418e-...::bank_holiday_premium). Those si | confirmed | **yes** |
| 150 | firing invariants run 'uncapped' (docstring: 'the per-metric cap holds'; caps li | fixture-invents | Production reads max_signals=3 and max_per_lens=2 from data/signal_lenses.json, and cap_briefing's third knob max_behind=2 from ordered_scale_routing.json `_david_ratified_2026_06_13.briefing_cap` — which the fixture does NOT rewrite. signals.py:1302 computes reserve_nb = max(0, max_signals - max_behind): production 3-2 = 1, fixture 999-2 = 997. | cap_briefing's ratified composition never runs. In production pass 1 admits at most 2 behind signals and exactly 1 reserved non-behind, then pass 2 fills by impact and pass 3 admits confirm signals only as a last resort. Under 997 reserved slots pass 1 admits everything and passes 2 and 3 are dead code. The home briefing a member actually reads is the 3-signal capped set; this gate audits a set no member is ever serv | confirmed | **yes** |
| 160 | all five firing invariants (the entire server half of the gate) | fixture-omits | No query params at all. parse_cut (app.py:786-816) therefore returns {"dim":"all","value":None} on every run, and apply_strategy = request.query_params.get("strategy") != "off" is always True. Production sends ?cut=all\|industry\|fte_band\|twin\|group with cut_value, and ?strategy=off from the 'Apply my strategy' toggle. The signal population is then inherited from a mutable per-org row: the demo org's orgs.default_c | Three paths are unreachable. (a) build_signals(strategy=None) — the degrade path every org without a completed strategy takes and the whole ?strategy=off toggle — is never exercised, because the demo org's org_strategy.completed_at is set. (b) Because sig_cut (group) never equals the requested cut (all), the alias branch at app.py:2400-2401, where signals ride the REQUESTED items/money/_get_block/_dom_align — the bra | confirmed | **yes** |
| 157 | every fired signal is routed / no verdict word / held metrics do not fire — i.e. | fixture-invents | One hardcoded demo account pins the audit to org 5e67fa8c-84b2-4be7-9f59-8556bbd6b6e7 (Retail & Consumer Goods, 50-249 FTE) and to that org's single frozen strategy dial-set: market_position=lead, location_approach=agnostic (provenance 'set'), family_position=over, reward_mix=cash, transparency=open/live, budget_direction=investing, risk_appetite=early, acute_pressure=bau. Production runs build_signals for 271 classi | signals.py:1155 `_strategy_field(strategy,"location_approach") == "agnostic"` is permanently TRUE, so every signal on a location_scoped metric gets _suppress=True and is dropped at signals.py:1187 before any check runs — for any org that is not location-agnostic those same signals fire and print. The family_position=='statutory' reframe (signals.py:1163) is permanently OFF. And only one answer profile is ever audited | confirmed | **yes** |
| 75 | open snooze is kept / expired snooze auto-returns to inbox / bulk dismiss sets m | fixture-invents | Production always scopes signal_actions to the session org and the session user_id (app.py:3476-3529 writes, app.py:2373-2383 reads) and keys rows by sig_id — question_id for a plain metric, qid::row_id for a matrix row (pages.js:2201 sidOf, signals.py:959 st.get(s['sig_id'])). The fixture picks an arbitrary unordered org (today 00000830-3d69-7fd0-0000-f7412b5cb6c9, NOT the demo org whose signals the rest of the file | The rows are orphans — they belong to an org with no signals in this gate and a user who never logs in, so the round trip that matters (signal_actions row -> _statuses at app.py:2380 -> s['status'] at signals.py:959 -> the pool filter at signals.py:1262) is never exercised. Nothing asserts that a snoozed signal actually leaves the briefing or that an expired one comes back; the four checks prove only that SQLite inse | confirmed | **yes** |
| 83 | expired snooze auto-returns to inbox (row cleared) | fixture-invents | The fixture executes its own copy of the statement instead of invoking production's. The real auto-return lives at app.py:2373-2377 inside GET /api/overview, keyed on the session org+user and run lazily on read. The comment on line 69 concedes this ('runs against LUMI_DB via the same read-path SQL app.py uses') — a copy, not the path. | app.py's lazy-clear could be deleted outright, or have its status='snoozed' / snooze_until predicate changed, and this check still passes — it is asserting on SQL the fixture itself just ran. The gate does call /api/overview 77 lines later, but as a different user, after these rows are gone, and asserts nothing about snooze at all. A snoozed signal that never returns to the member's inbox is exactly the defect this c | confirmed | **yes** |
| 92 | bulk dismiss sets many rows at once / bulk restore (Undo) clears them all | both | POST /api/signals/action-bulk is never called. The inline replica's conflict clause is DO UPDATE SET status='dismissed'; production's (app.py:3524-3529) is DO UPDATE SET status=excluded.status, snooze_until=NULL, updated_at=datetime('now'). Worse, the six ids are inserted fresh into a user namespace the fixture cleared at line 77, so the DO UPDATE branch never executes even once. | The only way production reaches ON CONFLICT — re-triaging a signal that already carries a status — is never exercised, so the snooze_until=NULL clearing when a snoozed signal is bulk-dismissed, and the updated_at bump, are untested. The endpoint's own contract is untested too: the 500-id clamp, the status whitelist and its 400, and the deliberate exclusion of 'snoozed' from bulk. 'bulk dismiss sets many rows at once' | confirmed | **yes** |

## qa_strategy.py — 13 candidates

| line | check | direction | divergence | consequence | conf | prints |
|---|---|---|---|---|---|---|
| 409 | the payload carries a risk list | fixture-omits | The probe org is created at :74 by POST /api/auth/register, which routes to _insert_member_org(conn, org_id) with firmographics=None (app.py:1212, 4974-4989) — so orgs.industry is NULL forever. app.py:6139-6141 calls derive_risks(..., sector=org.get("industry")), so sector is always None. Production orgs have completed the profile (PROFILE_CORE) before any document is opened, so sector is always a real industry strin | strategy_align.derive_risks risk 6b (wage_floor) is gated `if sector and any(k in sector.lower() for k in ("retail","hospitality","leisure","consumer")) and _pay_low` (strategy_align.py:471) — unreachable. Compounding: with zero answers and no gaps, risks 1 (affordability, needs invest), 2 (evidence, needs b["gaps"]), 3 (exposure, needs a "below" verdict) and 4 (concentration, needs >=3 gaps) are also unreachable, an | confirmed | **yes** |
| 437 | unit rates state a per-point value with the arithmetic attached | fixture-omits | orgs.fte_band is NULL on the probe org (same register path, firmographics=None). positions.money_opportunities:613 does `fte = a["fte_band_midpoints"].get(org["fte_band"] or "", None)` → None, and positions.compute_unit_rates:585 returns None on `if not fte`. app.py:6104 then ships `"unit_rates": None`. Production orgs have a stated fte_band, so unit_rates is always a populated dict. | The four checks inside `if _ur:` (:438 per-point value + formula, :444 no unit rate for a metric priced exactly, :447 basis stated, :454 attrition rate reconciles with its own formula) NEVER execute. The gate prints a skip note at :435 and passes. The unit_rates block prints in the document's cost section as the board's sensitivity table. The gate defers the arithmetic to qa_strategy_align section G, but the PAYLOAD  | confirmed | **yes** |
| 418 | the envelope reports how many gaps it actually prices | fixture-omits | The fixture's strategy at alignment time is exactly REQ ({market_position:lead, reward_mix:balanced, primary_objective:retain}) because the last strategy write is the bare PUT at :255, and PUT /api/strategy replaces every field. Against that, strategy_align._intent_holds returns False for all 9 coherence rules in data/strategy_coherence_rules.json, and with zero submitted answers every position commitment resolves to | No commitment can EVER have status behind_intent or contradicted. Therefore strategy_align.evaluate's rule loop (:279-298) never runs a single rule, _cond_eval is never invoked, and strategy_align.options_for (:97-142) — including the whole OVERSPEND/direction=="past" coverage_note branch and the visible_qids entitlement strip — returns []. app.py:6094's gaps_total is structurally 0 and money["priced"] is structurall | confirmed | **yes** |
| 425 | the ask's £ is the same figure as the envelope's (one number, one source) | fixture-omits | positions.money_opportunities iterates MONEY_METRICS and does `v = coerce_number(org_answers.get((q.id, row["row_id"])))`, skipping every row where v is None (positions.py:628-632). The probe org has no answers, so `any_data` is never True and `results` is always []. Production callers always pass a populated org_answers map from pos.get_org_answers. | money["items"] is always [], so the cost-table serialisation at app.py:6096-6101 — the per-item label/direction/to_p50_gbp/to_p75_gbp, the `category` lookup via getattr(org_visible_questions(org).get(i["question_id"]), "sub_power", None), and levels_covered/levels_total (the Board-only-gap guard added 2026-08-16) — never executes. investment_to_p50_gbp is always 0, so this check compares 0 == 0 and could not detect t | confirmed | **yes** |
| 469 | the schedule buckets every planned action, dropping none | fixture-omits | out["plan"] is `(st.get("document") or {}).get("action_plan")` (app.py:5926), written only by POST /api/strategy/plan. The gate never calls that endpoint, and could not: the probe org's insights are locked, so the route refuses (report.js:534 has the matching `reason === "locked"` branch). Production documents auto-build the plan on first open (report.js:545 autoPlan), so out["plan"] is normally populated. | `_plan` is always None, so app.py:6113-6120 takes the else branch and out["schedule"] is always []. horizon_bucket() and the HORIZON_ORDER grouping never run, the per-action `horizon_bucket` stamp is never written, and "unscheduled" — the bucket that exists specifically so an unrecognised horizon is admitted rather than dropped — is never produced. This check evaluates 0 == 0 and the companion at :473 evaluates all([ | confirmed | **yes** |
| 422 | the payload carries the ask, with a decision named | fixture-omits | With out["plan"] always None, `_now` (app.py:6125-6126, actions whose horizon_bucket == "this cycle") is always []. Production orgs with a built plan land in the `if _now:` branch. | The entire DECISION UNITS mechanism at app.py:6132-6161 is unreachable: `_alt_of` (grouping alternatives by the commitment their lever was offered against), `_units`, `_choices`, and all three composed sentences — including "approve the one action scheduled for this cycle — a choice between %d alternatives", the wording the comment records as "caught live when a plan rebuild landed here" — plus the dirty-version pref | confirmed | **yes** |
| 463 | a priced action says the figure is the gap's, not the action's | fixture-omits | `_plan = alp.get("plan") or {}` is always {} (no plan is ever built on the probe org), so `_priced` is always [] and the guard `len(_priced) > 1` is never satisfied. Production plans routinely offer two alternative levers against one gap, which is the exact condition this check exists for. | The check body never runs. The 2026-08-16 defect it pins — two alternative actions against ONE gap each printing that gap's cost, so a reader could add them into a double-count the envelope does not carry — is unverifiable by this gate under any circumstance. It reads as a passing gate because a guarded block that never opens produces no PASS and no FAIL line at all. | confirmed | **yes** |
| 489 | the decision is keyed by category AND lever | fixture-invents | The walk over alp["domain_blocks"] → b["options"] → o["levers"] → l["lever_id"] always finds nothing, because app.py:6045 builds each block's options as `[_opt_by_id[c["id"]] for c in _cs if c["id"] in _opt_by_id]` and options_for only emits blocks for behind_intent/contradicted commitments, of which the fixture has none. So the gate substitutes the hardcoded literals "Pay" and "QA-SYNTHETIC-LEVER". Production always | All six option-decision checks (:494-516) run against a lever that does not exist in the inventory. app.py:6122-6124 returns option_decisions as the raw stored JSON blob, so the round-trip at :502 ("the decision comes back on the document payload") is tautological — it re-reads what the previous call wrote and can never detect that the key the server writes (strategy_align.option_decision_key, "category\|lever_id") f | confirmed | **yes** |
| 409 | changing the peer group elsewhere updates the stated comparator | fixture-omits | The gate resets orgs.default_cut to NULL at :207 (app.py:2734 `UPDATE orgs SET default_cut=?` with None for cut=="all") and never sets it again — the later strategy PUTs at :215/:221/:227/:235/:255 omit "comparator_cut", so app.py:6332's `if "comparator_cut" in doc_in` is False and default_cut stays NULL. Meanwhile /api/strategy/alignment computes its cut from parse_cut(request, org) (app.py:5894), which reads query  | Every D052 disclosure branch is structurally unreachable. All three are guarded on `doc.comparator_label !== cutLabel`: the cover line at report.js:1415-1417 ("Stated peer group … — not the basis for the reads in this document; see Method and basis"), the Method-and-basis reconciliation sentence at report.js:2571-2576, and its nested size-band clause `money.fte_band && /FTE/i.test(doc.comparator_label) && doc.compara | confirmed | **yes** |
| 169 | comparator label matches the benchmark's own wording for that cut | both | The probe org's fte_band is NULL, so `_band` is ALWAYS the hardcoded literal "50-249" — a band the org does not belong to — and the only comparator dimension ever accepted is fte_band::. app.py:5142-5162 _validate_comparator has three accept branches (industry, fte_band, group) and app.py:2675-2693 _default_cut_info has three label branches. Production members pick from all three via the peer-group selector. | industry:: is never exercised at all — neither _validate_comparator's `choices = profile_choices()["industry"]` membership test nor _default_cut_info's `return raw, val, {"industry": [val]}` label path (note the industry label is the bare sector name, with no " FTE"-style suffix, so the label assertion pattern this check uses would not even apply). group:: is exercised only on its REJECT side (:157, "group::no-such-g | confirmed | **yes** |
| 128 | measure-options lists visible metrics with floor + caps | fixture-omits | This is called at :128, before the first comparator PUT at :167, so org_strategy.comparator_cut is NULL and app.py:5368 passes comp=None into strategy_measure_options. app.py:5186-5189 then keeps `cut = {"dim": "all", "value": None}` and only overrides it when comparator_cut starts with industry:: or fte_band::. Production members open this picker after their comparator is bound, so the industry/fte_band branch is th | pos.block_for(p, cut) at app.py:5194 always runs on the all-peers block, so the function's entire documented purpose — "an ADVISORY n/suppression read under the DECLARED comparator", the cut-dependent fact the docstring at :5177-5182 says is the reason it exists — is never exercised. The check compounds this by asserting only floor==5, max==8 and len(options)>100; it never inspects the `n` or `suppressed` fields the  | confirmed | no |
| 409 | an option decision can be recorded | fixture-omits | signals_mod.build_signals is called at app.py:5971-5974 with `items` from pos.position_items over an org with no answers, so it returns []. Production always passes a populated items list from a submitted org. | _sig_by_dom is always empty, so every domain block ships signal_count 0 and signals [] (app.py:6037-6042), and the 2026-08-16 consultancy-review sort at app.py:5993-5997 — ordering each domain's signals so the ones AGREEING with its verdict lead, the fix for a Wellbeing verdict printed above a contradicting signal — never executes. The peer-n rollup at app.py:6053-6060 is likewise empty. There is no check anywhere in | confirmed | **yes** |
| 284 | approval records what was unstated | fixture-omits | The bare PUT at :255 (`{"strategy": REQ}`, no document) wipes principles_json, constraints_json and population_targets_json (app.py:6355-6404 write them unconditionally from an absent doc_in), and :207 already cleared default_cut; no action plan is ever built. So at approval time app.py:5373-5384 _unstated_sections returns ALL FIVE names every run. Production approvals happen on documents where some of the five are s | unstated_json is always the maximal five-element list, so the check's assertion `len(ap["unstated"]) > 0` is satisfied by a constant. The partial case — the one a governance record exists to distinguish — is never produced, so neither the singular/plural composition nor the correctness of the per-section truthiness tests (e.g. `bool([p for p in principles if str(p).strip()])` vs a list of empty strings, or comparator | confirmed | **yes** |

## qa_strategy_align.py — 11 candidates

| line | check | direction | divergence | consequence | conf | prints |
|---|---|---|---|---|---|---|
| 371 | every derivable risk fires on a fixture that meets all seven conditions | fixture-omits | sector= is never passed at any of the 6 derive_risks call sites (grep for 'sector' in the gate returns nothing). Production app.py:6139-6141 calls derive_risks(..., sector=org.get("industry")) through the app-side binding at app.py:5871-5878, which forwards sector= to strategy_align.derive_risks. strategy_align.py:471 gates the eighth risk on `if sector and any(k in sector.lower() for k in ("retail","hospitality","le | The wage_floor risk (id="wage_floor", class="Statutory", strategy_align.py:484-496) is unreachable from this gate. The check asserts sorted(_ids) == exactly seven ids and reads green forever while the eighth risk exists. Worse, _rtext at line 382 is built from _RISKS, so the three string-hygiene checks that follow — directive-clean (383), legal-adjudication-clean (386) and R5 no-score (392) — never scan the wage_floo | confirmed | **yes** |
| 134 | counts reconcile: statuses sum to commitments, every category | fixture-omits | No evaluate() call in the file ever sets benefits_lead, reward_mix or location_approach on the strategy dict (confirmed by grep: zero hits for all three). Production always passes strategy_for_engine() (app.py:6461-6472), which populates all ten STRATEGY_ENUMS fields plus benefits_lead (a list, app.py:6469) and a provenance entry for each (app.py:6286). The fixture even supplies the EVIDENCE for the benefits_lead rul | Four of the nine shipped coherence rules can never be evaluated by this gate: P3 (reward_mix=cash), P4 (location_approach=agnostic), B2 (benefits_lead contains financial) and W1 (benefits_lead contains mental). I ran the fixture: the only rules that fire anywhere are I1, G1 and G2. Three engine branches are therefore dead in the gate — _intent_holds's `contains` leg (strategy_align.py:174-177, the only leg benefits_l | confirmed | **yes** |
| 69 | no verdict -> not_evidenced (never fabricated) | fixture-omits | cut_label= is never passed at any of the 14 evaluate call sites, so every one runs on the default "your peer group" (strategy_align.py:236). All three production sites compute a real label and pass it: app.py:5921 (_cut_label = cut.get("label") or "All peers" if dim==all else cut.get("value")), app.py:5424+5427 (board pack), app.py:4497 (_strat_review). The label reaches two reader-facing places in evaluate: the not_ | I ran the fixture: the Pensions & Savings commitment comes out as "You aim above market on Pensions & Savings; no market read is available yet on your peer group." with evidence={'source':'category market read','cut':'your peer group'}. Both strings print — web/js/report.js:1965 renders every commitment statement in the commitments register, and regBasis at report.js:1966-1968 prints "Market read on " + c.evidence.cu | confirmed | **yes** |
| 193 | options only for behind_intent/contradicted — evidenced gets none | fixture-omits | None of the 6 options_for call sites passes visible_qids=. Both production sites always do: app.py:5925 options_for(out["commitments"], visible_qids=set(org_visible_questions(org))) and app.py:5409 options_for(_al["commitments"], visible_qids=set(_visq)). visible_qids is the ONLY entitlement mechanism in this engine — strategy_align.py:132-133 pops prevalence_metric_id off any lever whose prevalence metric the org ca | The §2.5 entitlement drop is unreachable: with visible_qids=None the `if visible_qids is not None` guard is false at all 6 sites, so no lever is ever stripped and the gate cannot distinguish a working entitlement filter from a deleted one. The file's own docstring (line 7-8) claims "entitlement (invisible metric -> not_evidenced)" as covered; the one call that passes visible_qids passes it to a function that ignores  | confirmed | no |
| 369 | one-area risks read in the singular | fixture-omits | Every derive_risks call passes either _bl_r (exactly ONE hand-built block) or []. Production passes `blocks` from app.py:6003-6045 — one entry per hero domain, i.e. up to 8, built from hero_signals output. The key paths the fixture uses (name / count.strict / position.verdict / aim.stance) do match production, but the cardinality never does. | The plural branches of two risks that print are never built and therefore never enter _rtext for the directive/legal/R5 scans at lines 383-394: the evidence risk's "%d areas are carrying a recommendation on an indicative read" with "have ... their ... their" (strategy_align.py:398-406) and the exposure risk's "Each of these sits below the peer group..." (strategy_align.py:420). The check at 417 explicitly asserts the | confirmed | **yes** |
| 371 | every risk string is directive-clean (a risk is an exposure, not advice) | fixture-omits | The fixture passes one-key label maps. Production passes the whole vocabularies: budget_labels=STRATEGY_LABELS["budget_direction"] = {"investing":"one of investment","flat":"broadly flat","pressure":"under pressure"} (app.py:5087-5088) and constraint_labels=STRATEGY_CONSTRAINT_LABELS, six entries including "statutory and regulatory pressure", "collective bargaining", "systems and payroll change" (app.py:5096-5100). T | The affordability risk fires on budget in ("flat","pressure") but the gate only ever supplies "pressure", so the "You described the budget as broadly flat..." sentence — half the affordability text an org can receive — is never built or scanned. Likewise the constraints risk title interpolates _english_list(cons) from the label map; only the literal "affordability" ever reaches _rtext, so five of the six constraint l | confirmed | **yes** |
| 59 | ahead of a lag aim -> behind_intent (P2 overspend read) | fixture-invents | The fixture pairs target.alignment="ahead" with a strategy whose only stance is market_position="lead" and no domain_targets override. Production derives both from the same source: positions._market_target (positions.py:1337-1345) sets stance = domain_targets[sec] or market_position and align = "ahead" only when rank > aim; with aim=lead (2) and rank capped at 2, alignment can NEVER be "ahead". The fixture's target.s | I ran it: the commitment produced is "You aim above market on Time Off & Family; the live read sits past it — spend beyond the stated position." — an aim word and a direction production cannot pair. The overspend path (direction="past", which also drives options_for's lever suppression at strategy_align.py:112-121) is therefore only ever exercised through a strategy/domain combination no org can be in, and the senten | confirmed | **yes** |
| 56 | R3b — Wellbeing/Governance NEVER carry a position commitment | both | The fixture keys competitiveness as "competitive". positions.hero_signals emits "competitiveness" (positions.py:1452 and 1508), and app.py:4496 / 5426 / 5919 pass hero.get("domains") straight into evaluate with no remapping — the "competitive" key seen at app.py:2572 and 6013 is a DIFFERENT downstream structure that never reaches this engine. evaluate reads `d.get("competitive", d.get("competitiveness", True))` (stra | The competitiveness leg of the skip guard — the only leg production uses — is never executed, and no gate fixture ever presents a non-competitive domain, so the "a non-competitive domain gets no position commitment" contract is not tested in either key. Today the shipped data/market_position_config.json marks all eight domains competitiveness:true, so the branch is dormant in production too and this check passes on S | confirmed | no |
| 247 | an uncovered category still says so plainly (no silent cap) | fixture-invents | The commitment category is a literal that cannot occur. Production commitment categories come from exactly two sources: hero domain names (sub_power values, strategy_align.py:267) and rule["category"] (strategy_align.py:294), and the gate's own check at line 179 asserts the lever inventory covers all eight of them. Production also never passes levers= at all (app.py:5409, 5925), so `covered` is always the full 8-cate | The `if c.get("category") not in covered` branch (strategy_align.py:142-144) is unreachable in production, so the check verifies a note no member can ever be shown — and the note it asserts on is itself stale, still claiming "The lever inventory covers Pay, Benefits & Lifestyle and Time Off & Family at v1" after tranche 2 extended coverage to all eight. The check is green and load-bearing for nothing. | confirmed | no |
| 225 | covered category with no lever in the needed register is NEVER silently empty | fixture-invents | The fixture hand-builds a lever inventory with every Approach lever deleted. Production never passes levers= at all — options_for falls through to load_levers() (strategy_align.py:102), i.e. the full shipped file, which the gate's own check at line 184 proves carries BOTH registers in every category. | The elif-not-picks coverage_note (strategy_align.py:145-152) cannot be reached by any production call, so this check exercises a branch that renders for no member. The gate's comment acknowledges the fixture is synthetic; the audit point is that the guard is now proven only against an inventory that cannot ship, and the sibling check at line 240 ("no options block anywhere is both lever-less and note-less") includes  | confirmed | no |
| 328 | the attrition rate is 1%% of headcount × cost per leaver | fixture-invents | The assumptions dict is a three-key literal matching aggregate.DEFAULT_ASSUMPTIONS exactly, and fte is the midpoint of the smallest band. Production passes positions.get_assumptions(conn, org_id) (positions.py:557-563, reached via money_opportunities at positions.py:683), which starts from get_meta("assumptions_defaults", ...) — a DB-stored copy, editable by staff — then base.update(uj(row["assumptions_json"])) with  | The arithmetic is only ever verified against the hardcoded defaults at one band. An org_assumptions override that stores a JSON string or float for cost_per_leaver_pct_salary / agency_premium_pct flows straight into `a["cost_per_leaver_pct_salary"] / 100.0` and into the "%s%%" formula strings, and neither the crash nor a malformed formula line can be reached from this fixture. The unit rates print in the money sectio | speculative | **yes** |

## qa_strategy_doc.py — 9 candidates

| line | check | direction | divergence | consequence | conf | prints |
|---|---|---|---|---|---|---|
| 730 | G64 the alignment endpoint resolves twin blocks through pos.block_for | fixture-omits | Production (app.py:5951-5955) calls pos.block_for with THREE real arguments — `pos.block_for(payloads().get(qid) or {}, cut, (tb or {}).get(qid))[0]` — where arg1 is a live payload dict, arg2 the request cut, arg3 the per-question twin block from build_items (a dict on twin/group, None on all/industry/fte_band, and `{}` for every qid when twin_blocks_if_needed hits the suppression floor at app.py:829). The fixture pa | positions.block_for(payload, cut, twin_blocks=None) (positions.py:81) is never invoked, so neither argument ORDER nor argument VALUE is exercised. `pos.block_for(payloads().get(qid) or {}, cut, None)` — the twin block hard-coded absent, i.e. verbatim reference case #1 (qa_overview.py:73) — satisfies both predicates and passes G64. So does `pos.block_for(cut, payloads().get(qid), tb)` with the arguments transposed. Th | confirmed | **yes** |
| 761 | G65 a signal-builder failure fails the request, it does not blank every area / G | fixture-omits | Production ships a purpose-built fault-injection seam for exactly this assertion: app.py:5953-5956 raises RuntimeError when `?_qa_fail_signals` is set AND LUMI_QA_SEAMS=on, so a caller can prove the 500 propagates. The fixture never issues the request — it makes no HTTP call at all, and grep across every .py and .sh in the repo shows `_qa_fail_signals` appears ONLY in app.py:5954. The seam is fired by no gate in the  | The runtime half of P1-B is unexercised: that the re-raise actually reaches the client as HTTP 500 (not swallowed by an outer handler), that the `from e` chain survives, that the member-facing detail string renders, and that the log line actually formats without a second exception. A `raise` statement placed on a branch that cannot execute — e.g. inside `if False:` or after an unconditional `return` in the handler —  | confirmed | **yes** |
| 393 | A2.3 the stated pool size equals the live answering-org count | fixture-omits | The figure `stated` (A.get_meta("peer_pool").responding_orgs) is produced by aggregate.py:850-887 under THREE filters production always applies: (a) `WHERE a.snapshot_id=?` bound to CURRENT_SNAPSHOT=1 (load_answers, aggregate.py:822-826); (b) `JOIN orgs o ON o.org_id=a.org_id WHERE o.exited_at IS NULL` — the R6 pool-removal-at-day-0 rule; (c) `responding &= {org_id FROM orgs WHERE submission_complete=1}` — the ALLOW_ | The check compares the printed pool against a strict superset population, so it is not measuring the quantity it names. The three firewalls that DECIDE the pool figure are never exercised — break the exited_at join or drop the submission_complete intersection in aggregate.py and this check moves toward green, not red, because the unfiltered count is what a broken aggregate would converge on. Conversely a stale meta c | confirmed | **yes** |
| 419 | A2.1 the ruled provenance sentence appears verbatim | fixture-omits | The capturing group `(\d+)` extracts the pool number the member actually reads (270 in the delivered artefact) and the check then discards it — `bool(ruled)` only. Production computes that number from get_meta("peer_pool").responding_orgs (app.py:6216). No check in this file, or in any half of it, compares the artefact's printed number to either the meta value or the DB. A2.3 (:396) compares meta to a raw DB count an | D002's literal defect — a stale pool figure surviving in a delivered document ("220 outlived the pool's growth to 270 on every stale surface in the estate", the gate's own words at :394-395) — is owned by no assertion that reads the delivered number. The artefact can print 220 while the DB holds 270 and every D002-owning check in this file passes: the sentence SHAPE is verified, the sentence's only fact is not. This  | confirmed | **yes** |
| 409 | the whole --pdf half (32 call sites: A1.1/A1.2/A1.3/A1.5, D029/D033/D066/D068/D0 | both | Production renders this document per-request: report.js:538 fetches /api/strategy/alignment, which runs build_items → prevalence_items → practice_position_items → hero_signals → strategy_align.evaluate → options_for → money_opportunities → build_signals for the calling org under parse_cut. The fixture opens a frozen 40-page file that no build step regenerates — run_gates.sh:56 states the rule explicitly ("R-a: the ga | Nothing binds the artefact to the report.js the gate source-checked ten lines earlier, or to the libraries it just scanned, or to the DB it just queried. Edit any renderer in report.js and all 32 artefact checks keep reading yesterday's bytes and stay green — the source half moves, the artefact half cannot. Concretely: A1.5's PDF-side ask reconciliation, D068's superseded-description sweep, D072's per-area cardinalit | confirmed | **yes** |
| 287 | A4.1 no directive language / no legal adjudication in the lever/rule libraries | fixture-omits | Production never applies these two guards alone. The per-domain commentary — app.py:5934 calls it "the MAIN part of the report" — passes validate_domain_summary (claude_api.py:1544-1596), which runs EIGHT regex guards in sequence plus number-grounding: DOMAIN_RATIO_RE, DIRECTIVE_RE, DOMAIN_ADVICE_RE, LEGAL_RE, DOMAIN_BADVOCAB_RE, DOMAIN_LEGACY_RE, DOMAIN_ALARM_RE, DOMAIN_MKTPOS_RE. The board-pack narrative (validate_ | Six guards production always runs are never applied to the lever and rule prose, and that prose prints VERBATIM in the document's largest tables — report.js:1927/1934 and :2192/2194 render l.what_it_is and l.trade_off in the options exhibits, and :2243 renders descOf[a.title] (what_it_is) as the plan's own sentence. The classes left unguarded in the library are exactly the ones the levers are written in: DOMAIN_ADVIC | confirmed | **yes** |
| 302 | A4.1 / A4.3 / A4.4 / A4.6 — every check driven by lever_strings() | fixture-omits | The fixture scans rule['statement'] as the RAW TEMPLATE straight from strategy_coherence_rules.json. Production never renders that string: strategy_align.py:288-289 always substitutes before it reaches a page — `.replace("{answer}", matched or "").replace("{answer_detail}", matched or "your response")`, where `matched` is the member's own stored answer text from _cond_eval. Rule P3's statement carries a live `{answer | The composed sentence a member actually reads in the commitments register is scanned by no library guard. The directive, legal, vendor and forecast sweeps all run against a string containing a literal `{answer}` token instead of the interpolated text; the degenerate `matched or ""` substitution (empty answer → "(, your response)") is a typography defect of exactly D025's class, produced by a naive .replace() — the sa | confirmed | **yes** |
| 570 | G54 against-aim read <-> register position row / G62 a past-aim area renders no  | fixture-invents | Production never uses a literal area list. app.py:5904-5905 computes it for every request as `sec_order = [s for _, s in sorted({(q.sub_power_order, q.sub_power) for q in _q_all.values() if q.sub_power})]`, where `_q_all = org_visible_questions(org)` — a set that narrows per org by the hospitality/retail tronc module and by data/sector_scopes.json (app.py:365-385). The fixture hard-codes eight names and eight orderin | G54 and G62 fail OPEN on any drift between the literal and the stored taxonomy. G54 computes a symmetric difference of two sets both built by matching AREAS against the text — rename or re-case one sub_power (this repo carries migrate_diff1_domain_remap.py and migrate_b1_sub_power_order.py, so it has happened) and both sets go empty, the symmetric difference is empty, and D077's check reports green over zero areas ex | confirmed | **yes** |
| 545 | D042 area order is the stored taxonomy order | fixture-omits | Production always passes hero_signals a real, ordered `section_order` list as its third positional (app.py:5908), built at :5904-5905 by sorting on `q.sub_power_order`. The fixture calls nothing — it asserts that the 30-character substring `_q_all.values() if q.sub_power` occurs somewhere in app.py. That substring is the comprehension's SOURCE clause; it says nothing about the sort. It also has the pages of the artef | The ordering itself is unasserted. Delete `sorted(...)` and the set-comprehension emits arbitrary hash order; change the sort key from `q.sub_power_order` to `q.sub_power` and the areas go alphabetical — the substring survives both edits and the check stays green. D042 exists because a reviewer counted four different area orderings in one paper (the comment at app.py:5897-5902 says so), and the assertion that closed  | confirmed | **yes** |
