# Scoping session — the work remaining on the reward document

Ten items scoped against the source, one investigator each, told to say plainly when the
reported premise does not hold. **Two did not.** Sizes are one reviewable diff (small), a
class with its own gate (medium), or an engine/data-model change (large).

Everything below is the investigators' reading of the code. The headline claim on each was
spot-checked; where a figure is theirs and unverified it says so.

---

## The one thing to do first

> Wire the existing `server/verify_report_pdf.py` into `run_gates.sh` across all five cut shapes (all / industry / fte_band / twin / group) plus one synthetic 60-character group label, and run it. It is red at HEAD today — 7 failures on industry, 7 on group — so it proves F-000 in the same afternoon it is built, and it costs no new tool: the verifier already owns every symptom (page count vs footer claim, footers running 1..N, contents entries resolving), it is simply named in a COMMENT at run_gates.sh:243 and has never been pointed at anything but the all-peers artefact (DOC_PDF default, run_gates.sh:47). Do this first because eight of the ten items end in a web/js/report.js diff on sheets already measured 89-97% full, this project has twice lost content off a sheet by adding a line, and without this arm every subsequent fix is verified by hand on one of five cuts. Send David the single ruling sheet the same day (six questions, listed in Wave 1) so his clock runs while Wave 2 ships.

That recommendation is **self-verifying**: if the gate goes green on all five cuts the
concern was overstated, and if it goes red it has already paid for itself. It needs no
ruling and it is the cheapest thing on the list.

---

## Waves

### Wave 1 — The net, the record, and one ruling sheet

**Size:** 1 to 1.5 days. Gate arm ~half a day including five renders; the qa_overview pass ~45 lines and +0.15s of runtime; corrections and sheet ~2 hours. Do not loop qa_hero into the cut roster — its wash-out threshold asserts at_share < 0.70 and twin reads 0.66, so a naive loop goes red for no defect and burns the red-first discipline on noise.

**Why these together:** These are the only four things on the list that need no ruling and unblock everything else. The two gate items are one class, not two: no gate in the tree exercises a non-default cut — not in the renderer (one artefact, all-peers) and not in the engine (twin blocks are reached by nothing; qa_overview pins cut=all and tb=None). That is the exact gap that shipped P0-7f, where a dict called as a function went silently dark on five of eight signal mechanisms and the whole suite stayed green. The record corrections are free and are the highest-value output per minute here — the D087 planning decision is currently being taken on the pre-477afe4 renderer, and the 'group only' row would let someone ship the industry cut. The ruling sheet goes now because five of the ten items stall on David and nothing else does.

**What a reader gets:** Nothing visible in the document — deliberately. What it delivers is that 'verifier 0 failures, suite green' stops being a claim about one peer cut and one code path, and becomes the acceptance test the next three waves are measured against. It also hands David his decisions on day one instead of day five.

Items:

- F-000 gate arm: wire verify_report_pdf.py into run_gates.sh over five cut shapes + a 60-char synthetic label; demonstrate RED at HEAD (item 9, step 1 only — no renderer change yet)
- Arm C, the 45-line version: in qa_overview.py add a twin-cut pass over the cut-invariant checks (1a/1b/1c, 5a, 7, 12a, 14a) built from A.twin_blocks_if_needed, skip-with-reason if the org has no similarity vector; and rebuild the signals half on A._org_sweep_cut so the gate asserts the 57-signal group set the product actually ships instead of a 55-signal all-peers set it does not (item 8, smaller version)
- Correct three records that would otherwise steer a decision wrong: D087 is 49 of 55 deferred and 6 printed, not 43 of 55 and 12 (stale since commit 477afe4 cut _room 2→1); F-000 is NOT 'group only' — industry fails earlier and harder (FORMAT_AUDIT_2026-08-17.md:36, CLOSE_OUT_2026-08-17.md:85); DIAGNOSIS_GEN_VERSION (claude_api.py:1791) is dead — the cache keys on DIAGNOSIS_VERSION, so fix it or delete it before someone bumps the wrong one
- Issue ONE ruling sheet, six questions: (i) F1/F2 family-provision rule text; (ii) may the peer group be named once (cover, running footer, Method and basis) instead of restated in four fitted sites; (iii) Exhibit 2 aim marker — bracket or delete, taken together with R-C; (iv) lift DECISIONS.md:20047 for D088 and choose conditional vs unconditional wording; (v) the aim-inheritance disclosure — do Wellbeing and Governance, which have no stated aim of their own, carry the global 'lead' stance; (vi) the D071 reversal, since it was itself an external-review fix

### Wave 2 — One pass over report.js: the sentences that are wrong

**Size:** About 2 days including staged per-class commits, the render and the gate cycle. The real risk is pagination on the tightest sheets in the paper — which is precisely what Wave 1 built the arm to catch.

**Why these together:** Five separate findings, four of them graded BLOCK by the readers, all landing in web/js/report.js as copy or renderer changes. None touches the engine, the payload, a cache version, the AI path, or reverses any ruled behaviour — which is exactly why they belong together and ahead of everything expensive. They share one ?v= bump, one PDF render, one Gate A --pdf run and one pagination check; shipped separately they cost five of each. Two ordering notes: choose D088's unconditional wording specifically so it stops depending on the D087 outcome in Wave 4 (if _room later rises, the clause is still true and the gate stays a pure PDF-text assertion); and D088 needs DECISIONS.md:20047 lifted, which is question (iv) on the Wave 1 sheet — a one-line approval, not a decision.

**What a reader gets:** The pages a remuneration committee actually argues over stop asserting things the system cannot support. §08 no longer presents four of seven divergences under a heading that promises all of them. The only £ figure in Part B carries its population and points at the section that qualifies it, so it can no longer be read as a whole-company one-off. The method page stops explaining the verdict/percentile split with a cause that is not the cause. The page that declares Pay overspending against its own aim keeps the sentence saying the aim is the open question, instead of following the declaration with two cost-neutral governance levers. And the one area asserting 'on market' where nine of nine signals disagree says so.

Items:

- §08's scope claim: the standfirst at report.js:2272 says 'Where the declared strategy and the live position diverge' over a list truncated to four by strategy_diag.py:154. Either name it as the most material N with a derived pointer to the other divergences, or drop the completeness claim. Copy only — the cap stays (item 7, step 1)
- The £27,000 population, client-side: the strategy-document payload already carries levels_covered/levels_total per priced item (app.py:6109). In the Findings card (report.js:2282-2288) look the metric up in money.items by its payload label and append one deterministic line stating the population and pointing at What it costs — outside the model's text, so it cannot be dropped by narration (item 3, smaller version)
- The false explanation in §03: report.js:2768 says 'near the band's edge the two can differ.' It is not a hedge, it is a wrong cause — an area at depth 32 can read on market three points clear of the edge, and an all-tie-at-median area diverges nowhere near an edge. Replace with the count-versus-depth sentence, and add the two Gate A assertions plus one engine-side synthetic-pool check, because nothing in server/qa_*.py owns either explanation today (item 5, cheapest rung)
- The past-aim renderer defect: report.js merges levers across a domain's option blocks (1894-1896) and prints the coverage note only when the merged list is empty (2073), so a domain with a past-aim gap AND a coherence gap prints both gap statements, offers levers that answer only the other one, and drops the only sentence that questions the aim. Same shape at the deck sentence (1397-1400), which needs every(g => g.direction === 'past'). G62 already WARNs on it: 'past-aim areas still offering levers: [Pay]'. Fix, then flip qa_strategy_doc.py:686 from advisory to blocking, and fix the qa_strategy_align.py:78-79 fixture to a lag aim with an above-market read (item 2, step 1)
- D088, in its unconditional form: edit only the folded-branch sentence at report.js:2000-2003 so it always states how many of the named signals agree — '…is flagging 9 signals; none of them reads on market, and they are set out in full under Time off & family in the app.' Add an assertion that the agree-first sort at app.py:5978-5988 exists, because it is what makes a 4-row client test a truthful claim about 9 (item 4, smallest version)

### Wave 3 — The two ruled diffs

**Size:** About a day of code once ruled; the wait is the ruling, not the work. Expect visible movement in already-approved artefacts across the estate when F1/F2 fire — that is the correct outcome, but say so before it happens.

**Why these together:** Both are single small diffs whose only blocker is David's wording, both are already proven against real renders and the real engine, and both change what an already-signed-off document says — so they should be recorded rather than quietly applied. Batching them means one ruling round-trip rather than two. The F-000 half is the genuine ship blocker in the whole list: the document is only known-good on the default cut, and the cause is now isolated to one customer-typed string of 1-60 characters sitting inside regions fitted by item count rather than measured height. Note also that the F-000 edits land on the same headings and captions as the still-open R-h format questions (F-001/F-002/F-003) — if those get ruled in the same round, do them in one pass.

**What a reader gets:** The paper survives every peer cut: 'Page N of M' stops being wrong and three contents entries stop pointing at the wrong sheet on the industry and group cuts. And the document's worst leak — a strategy asserting family provision is deliberately more generous while its own register says the area is aimed below market, alongside 'pay is fully open' against 'pay is never in adverts' — is caught by the engine and printed on the Tensions page and in the register, instead of being caught by a reviewer.

Items:

- F-000 renderer half: replace the interpolated cutLabel with a fixed-length referent at the four fitted sites — domain deck sub (report.js:1928), signals exhibit caption (1118), domain commentary (1364), register READ FROM cell (2104). Proven, not estimated: the group-cut payload rendered with only the label shortened gives 38 pages and 0 failures, identical to all-peers. Do NOT retune the chunk budgets or _room (item 9, smaller version)
- F1/F2 family-provision coherence rules: append two objects to data/strategy_coherence_rules.json using the shipped strategy_stance grammar (strategy_align.py:201-214, precedent P6) — family_position 'over' against a lag aim on Time Off & Family, and its mirror, family_position 'statutory' against a lead aim. Verified against the unmodified engine on four paths; the file hot-reloads on mtime, so it is a JSON append, not a deploy. Ship with a Section-G gate block mirroring P6's, and tighten qa_strategy_align.py:287-290 so every strategy_stance domain string must be in the canonical sub_power set — a mis-cased key silently falls back to the global dial and reads plausibly wrong (item 0)

### Wave 4 — Engine work, sized only after Wave 3 lands

**Size:** 4 to 6 days, and genuinely re-size it after Wave 3 — two of the three depend on rulings whose answers change the shape. If the aim-inheritance ruling goes the other way (an area with no stated aim carries no aim), two of the seven divergences leave §08 and the cap question changes shape entirely, which is the argument for not opening with this work.

**Why these together:** These three are one class for the reason that matters operationally: each changes what the ENGINE emits, therefore what the model narrates, therefore what must be cache-invalidated and re-verified on the AI rig — and run_gates.sh forces ANTHROPIC_API_KEY empty, so the normal suite cannot see the failure mode where the model quietly drops a clause. Two of them touch the same diagnosis roll-up at app.py:2567-2585. The annex adds sheets, so it can only land after Wave 1's cut arm and Wave 3's label fix — adding paper on top of a cut that already spills is how this class of defect compounds. This wave also contains the only reversal of a prior external-review fix in the whole plan (D071), which is why it is last and why it needs the ruling recorded, not assumed.

**What a reader gets:** §08 stops omitting three of seven divergences, including the area carrying the paper's only two Contradicted rows. The population clause becomes true on every surface rather than only in the PDF, so the document and the Overview diagnosis stop disagreeing. And the reader gets all 55 signals for 5-8% more paper, instead of the 6 the paper currently prints while naming 55.

Items:

- §08 coverage: drop or parameterise strategy_diag.py:154's [:4], and repair the sentence the cap was silently holding up — deterministic_diagnosis at :202 asserts 'N areas sit below market against the position you set' counting every gap finding, which is true today only because the cap happens to cut before the first gap that is not below market. Compose the clause from the findings' own values, invert prompt rule 7 (claude_api.py:1754-1759) and the D071 regex (1836-1845), bump DIAGNOSIS_VERSION, suppress the duplicate p15 'sit above a deliberately lower aim' sentence for areas that now carry a card (item 7, step 2)
- The population clause, engine-side: keep levels_covered/levels_total on the winning item through the roll-up at app.py:2581-2584 (four tokens), state it in strategy_diag.py:136 in BOTH the partial and complete cases, and add a validate_diagnosis rule so the clause must survive narration — otherwise the live AI rig silently reverts to the unqualified sentence and a deterministic gate run shows green over it (item 3, steps 2-4)
- D087 signals deferral: lift the [:4] payload cap at app.py:6051 as a standalone diff with the deferral gate written red first (it parses the two 'N signals … in the app' sentences out of the rendered PDF and asserts the summed deferral against whatever David rules), then print the compact one-line-per-signal annex — all 55 rows, grouped by area, in Part D via the existing Prun idiom, 2-3 sheets. Rule the dismissed/snoozed question first: cap=False skips that filter, so a wider print puts triaged-away signals in front of a board (item 6, smaller version)

---

## Dropped — and why

A scoping session that recommends everything has not scoped anything.

- The Exhibit 2 aim BRACKET or shaded zone (item 1's headline version). Build nothing; either delete the marker (~12 lines removed: RR_AIM_PCT at report.js:801-802, the triangle at 425-427, the row payload at 2225, the legend swatch and its CSS) or leave it as-is pending R-B/R-C. The bracket is the more informative picture and it manufactures a fresh picture-vs-prose contradiction on the one exhibit
- Item 5 as scoped — 'state the verdict-vs-percentile rule where the puzzle appears'. The premise is false: the rule is stated in §03 on page 4 of every copy since the Method section moved to the front, and again in full and correctly under the one chart that puts two areas on the same ruler. Scoping it as written buys work that was already bought. Only the one false clause and the missing gate are 
- Item 7 step 3 — routing Contradicted commitments into §08. It is a different engine behind a different endpoint, and the coverage half plus Wave 3's F1/F2 rules already put contradictions in front of the reader on the Tensions page and in the register. Do it only if David rules that §08 must be the single place divergence lives; it is a separate question from the cap and should not be smuggled int
- Item 2's two structural versions: making the aim a recordable decision (a third register or a pseudo-lever), and splitting 'past' out of behind_intent. The first couples the aim question to the plan, the schedule, the £ envelope and the board's approval list — a product decision, not a document fix. The second is the R5 four-status contract and every count site in two renderers; it is the right lo
- Item 8's shared qa_cuts.py roster and multi-gate adoption. The sweep's 26-37 rows overstate how much of it is one mechanical change, and the real prerequisite is on the production side: five hand-rolled cut constructors (app.py:786, 2659, 3915, 7170, 7355), one of which is already missing parse_cut's unknown-vocabulary validation. Collapsing those onto one cut_from() is the change that makes cut c
- Item 6's full-detail 5-6 sheet appendix (13-16% more paper for what the compact annex delivers in 2-3), and — separately and emphatically — the copy-only version that rewrites 'set out in full in the app' into a friendlier sentence. The second is the cheapest thing on the entire list at about an hour, and it renames the problem instead of closing it while burning the ruling that would have closed 
- Item 4's tidy restructure (hoisting the caveat out of the report.js:2004-2049 ternary), and any raise of _room to buy print rows. The restructure moves the paragraph D072's 700-character scan window reads, on the tightest sheet in the paper, for a tidiness no reader sees. Raising _room is the configuration that was measured running 41px past A4 on that exact sheet, and it costs a page split plus e
- Item 3's per-level £ restatement and naming the levels in the finding sentence. It needs rows plumbed onto a payload that deliberately drops them, and it is a different document decision about how much arithmetic a Findings card should carry. Counts answer the reader's question; names are a want. Also reject the tempting half-measure of making report.js:2619 unconditional — it improves the cost ta

---

## The items, as the code actually has them

### D087 — 43 of 55 signals deferred to "the app": establish the population caps, cost a full-signal appendix, and name the cheaper options

**Premise holds:** **NO — see below** · **Size:** medium · **Needs a ruling:** yes

THE MECHANISM IS REAL. BOTH NUMBERS IN THE FINDING ARE WRONG, AND IT IS WORSE THAN REPORTED. 1. "Each area page prints at most 2 signal rows" is not what HEAD does. `web/js/report.js:1914` reads `const _room = inlineFollow ? 0 : 1 - (cntStrictFalse ? 1 : 0)` — at most ONE row, and zero in two cases: an area whose follow-up rides inline on the read sheet (`inlineFollow`, report.js:1890), and an indicative area (`cnt.strict === false`, fewer than DOMAIN_MIN_POLARISED metrics). The surrounding comment still says "2, not 3" and is stale. Commit 477afe4 (2026-08-17 14:13) cut it 2 → 1 to pay for the RrMixBar area chart added to the same sheet; `git grep` confirms 17357b2 and a938efb carry `2 -`, 477afe4 onward carry `1 -`. 2. "43 of 55" is stale by that same commit. PHASE2_REPORT_2026-08-17.md §4.2 cites `report.js:1804`, i.e. the room=2 renderer, and CLOSE_OUT_2026-08-17.md:88 inherits it. Measured directly out of the delivered artefact `artefacts/total_reward_strategy_and_plan.pdf` (written 17:04 today, 38 pages, HEAD renderer): Pay 1 of 9 · Pensions & savings 1 of 9 · Benefits & lifestyle 1 of 9 · Incentives & recognition 1 of 4 · Wellbeing 1 of 5 · Governance & transparency 1 of 10 · Time off & family 0 of 9 (whole exhibit folded) · Health & protection 0 signals. 6 printed of 55. 49 deferred, not 43 — 89%. The "What sits above market" list (report.js:2315, `.slice(0,3)`) prints nothing in this artefact, so 6 is the whole of what a reader sees. THE THREE POPULATION CAPS Cap 1 — the briefing cap, `server/signals.py:1280-1283`: `cap_briefing(pool, max_signals=5, max_per_lens=2, max_behind=3)` applied separately to the market and practice pools. NOT IN FORCE for this document: `server/app.py:5959` passes `cap=False`, and `signals.py:1257` returns the full impact-sorted set. It governs the in-app briefing and the board pack only. Note the cap=False branch also skips the dismissed/snoozed filter that only exists at `signals.py:1264`. Cap 2 — the payload cap, `server/app.py:6051`: `for x in _sigs[:4]` — four per area on the wire. `signal_count` at `app.py:6036` is the UNCAPPED `len(_sigs)`, which is exactly why a sheet can honestly say "(1 of 9)" while the client holds only four. Ceiling: 4 × 7 areas with signals = 28 of 55 reachable by any renderer change alone. Cap 3 — the print room, `report.js:1914-1915`: 1, or 0. This is the binding constraint. Lifting cap 2 on its own changes nothing a reader sees. (Two minor relatives: the board pack takes `[:5]` across ALL areas at `app.py:4581`; the above-market list takes 3 at `report.js:2315`.) WHAT AN APPENDIX WOULD 

**Where it lands:**

- `web/js/report.js:1914`
- `web/js/report.js:1915`
- `web/js/report.js:1890`
- `web/js/report.js:1999`
- `web/js/report.js:2046`
- `web/js/report.js:2034`
- `web/js/report.js:2315`
- `web/js/report.js:959`
- `web/js/report.js:988`

**Risk:** WHAT BREAKS 1. A4 spill. Every room change in this file's history was made by print measurement, and one of them (Time off & family at two rows) ran 41px past the sheet. Adding sheets also lands on top of F-000, still open: the group cut already renders 41 physical pages against a footer claim of 40, and it is a declared blocker on any cut but all-peers shipping. 2. Dismissed signals in a board paper. `cap=False` skips the dismissed/snoozed filter (signals.py:1257 vs :1264) and the payload never carries `status` (app.py:6046-6051). An appendix would print signals the org has already triaged away in-app, and none of the three caps would catch it. Needs a status filter or a ruling BEFORE anything prints more rows. 3. D088 moves without resolving. The thin-evidence caveat is gated on `sigShown.length` (report.js:2034), i.e. on rows printed rather than rows existing. Any room change re-shuff

**Depends on:** F-000 — the group cut renders 41 physical pages against a footer claim of 40 (FORMAT_AUDIT; D088 — the thin-evidence caveat is gated on rows printed (report.js:2034) rather than rows; A ruling on triage status in the paper — cap=False skips the dismissed/snoozed filter (sig; A ruling on the print-room constant itself — 3 → 2 → 1 was set by print measurement across; verify_report_pdf.py is not wired into run_gates.sh (run_gates.sh:243) — the pagination ne

**Recommendation:** DO A SMALLER VERSION, IN TWO STAGES, AND TAKE A RULING ON THE NUMBER FIRST. Before anything is built, correct the record: D087 is 49 of 55 (89%), not 43 of 55, and the paper prints 6 rows, not 12. A planning decision taken on the old figure is being taken on the pre-477afe4 renderer. That correction is free and is the highest-value output here. Then: (1) lift the `[:4]` payload cap at app.py:6051 as a standalone diff — it is one line, it is invisible in print because room is the binding constraint, and it unblocks every downstream option; ship it with the new deferral gate written red first. (2) Print the compact deferred-signals annex (2-3 sheets) in Part D beside Governance via the existin

**Cheapest honest version:** Cheapest honest version that actually closes the reader's gap: a COMPACT SIGNALS ANNEX — one line per signal (Signal / Yours / Reads, detail dropped), grouped by area, all 55 rows, printed in Part D beside Governance (report.js:1045-1056) via the existing `Prun(title, body, arr, weigh, budget)` chunker at report.js:879. ~20pt/row → 2-3 sheets, +5-8% on 38 pages. Requires the app.py:6051 `[:4]` lift; needs no new pagination machinery, no per-area room logic, no change to the area sheets, and no change to D088. Cheaper still, and genuinely useful on its own: the payload lift ALONE (app.py:6051, `_sigs[:4]` → `_sigs`) plus the red-first deferral gate. Zero visible change, zero page cost, no ris

### Stating the verdict-vs-percentile rule where two areas at the same percentile carry different verdicts

**Premise holds:** **NO — see below** · **Size:** small · **Needs a ruling:** no

The ARITHMETIC in the finding is real and I reproduced it against the live functions. The "never stated" half is false, and it is false at exactly the page the finding describes. The engine. `_market_class` (server/positions.py:941-951) buckets ONE metric by its polarity-adjusted percentile against MARKET_BAND_LOW/HIGH = 35/65 (server/app.py:108-109) — plus a median-tie override: any metric whose tie block spans P50 counts "at" whatever its midrank. `_pool_verdict` (positions.py:954-981) then produces TWO independent numbers from that pool: `lean = (above - below)/pool`, which bands the verdict word at VERDICT_NET_LEAN = 0.25 (app.py:135-136), and `depth_pctl`, the MEDIAN of the adjusted percentiles. Nothing ties them together. Run against the real function with band 35/65, margin 0.25: A = adj percentiles [20,25,32,70,80] -> verdict "at", depth_pctl 32 B = adj percentiles [30,31,32,33,34] -> verdict "below", depth_pctl 32 C = three metrics at adj P32, each tie-at-median -> verdict "at", depth_pctl 32, and every metric counts "on it" So yes: two areas can both print "around the 32nd percentile" and read differently. Three separate mechanisms do it, not one. Where the document already says so. The finding's tableau — two areas side by side on one ruler — exists on exactly one page, "Position at a glance", and that page carries a note under its legend (report.js:2255-2262) saying precisely this: dot = depth percentile, colour = verdict across every metric, "the two can disagree: an area can sit outside the middle band and still read on market." That was shipped as F-014. And "Method and basis" is now section 03 on page 4 (report.js:904, commit cb6aa22 moved it to the front), stating the rule before the paper asserts a figure. So the premise as written does not hold. What IS actually wrong, and it is smaller and sharper. (1) The §03 sentence is factually incorrect about the mechanism: report.js:2768 says "its percentile is depth alone — near the band's edge the two can differ." Case A above diverges at depth 32, three points clear of the 35 edge; case C diverges nowhere near an edge at all. The document's only up-front explanation attributes the divergence to a cause that is not the cause, which is worse than saying nothing — a reader who takes it at face value will trust a P32/"on market" pair only when it sits at P34. (2) The eight per-area sheets, where the verdict word and the percentile sit in the SAME stat tile one line apart (report.js:1948-1951) and in the same sentence (report.js:1362-1366), carry no local reconciliation and no pointer back to §03.

**Where it lands:**

- `server/positions.py:941-951`
- `server/positions.py:954-981`
- `server/app.py:108-109`
- `server/app.py:135-136`
- `server/app.py:115`
- `web/js/report.js:2765-2770`
- `web/js/report.js:2255-2262`
- `web/js/report.js:1948-1956`
- `web/js/report.js:1963-1979`

**Risk:** No engine exposure at all — positions.py and app.py are untouched, so no verdict, count, gauge or £ figure moves. Three real risks, all on the document side. Pagination. This paper has already lost content off a sheet once from added marks (report.js:1982-1986 records the split bar being removed because it pushed Pay and Time past the sheet in print). Adding a paragraph to eight area sheets is the same class of change. Caught by server/verify_report_pdf.py, which owns pagination, and by the `--pdf` form of Gate A (server/qa_strategy_doc.py:19-22) — neither runs in the no-PDF gate, so this one has to be run deliberately. Boilerplate voice. A standing caveat repeated verbatim on eight sheets is the exact defect this document already fixed once — report.js:1994-1996 records the generic sizing-up caveat printed under every finding, flagged by five of seven reviewers, and moved to a single si

**Depends on:** Nothing blocking. The wording is copy in a David-signed-off document, so by this engagemen; If the per-area half lands, server/verify_report_pdf.py and the --pdf form of Gate A must 

**Recommendation:** Do the smaller version — the §03 sentence fix plus the two gate assertions — and drop the rest of the item as reported. The reason is that the finding's headline is wrong in a way that changes the size of the job by an order of magnitude. It says the rule is never stated where the puzzle appears; the rule is stated on page 4 of every copy of this document (report.js:904, moved to the front on 2026-08-17) and again, in full and correctly, directly under the one chart that puts two areas on the same ruler (report.js:2255-2262). Scoping this as "the document does not explain its own verdict rule" would buy work that was already bought. What is genuinely left is one wrong sentence and one missin

**Cheapest honest version:** Two rungs down, both honest. Cheapest: fix only the §03 sentence at report.js:2768 — replace "near the band's edge the two can differ" with the count-versus-depth sentence. One string, one file, zero layout change, zero pagination exposure, and it is a defect fix rather than new copy: the current sentence misstates the mechanism, demonstrably. Add the two Gate A checks and the engine-side synthetic-pool assertion. That is the whole of the real work and it is well under a day. One rung up, if the per-area sheets are judged to need it: make the note CONDITIONAL rather than standing — print it in the existing `rr-src` slot at report.js:1965 (where the "* indicative" note already lives, so no ne

### direction="past" (overspend against a deliberately lower aim) carries no option — should an explicit "re-set the aim" recommendation exist?

**Premise holds:** yes · **Size:** medium · **Needs a ruling:** yes

The premise holds at the engine level, but it is understated in one direction and wrong in another, and both corrections change what the work is. WHAT THE ENGINE DOES. `options_for` short-circuits on `direction == "past"` before any lever matching (server/strategy_align.py:112-122): it emits a block with `levers: []` and a category-interpolated `coverage_note`. `direction` is set at server/strategy_align.py:262-263, where `alignment == "ahead"` maps to `behind_intent` + `"past"`. The aim/alignment pair comes from `positions._market_target` (server/positions.py:1329-1345): rank {below:0,at:1,above:2} vs aim {lag:0,match:1,lead:2}, `ahead` when rank > aim — so it is only reachable on a lag or match aim, never on lead. It is live: today's artefact has two (Pay, Time off & family). The suppression is a frozen ruling, not an oversight — DECISIONS.md:18456-18464 (2026-08-15) records it as a defect fix ("the page was recommending more spend to fix over-spend"). CORRECTION 1 — it does NOT offer nothing. The coverage_note already says the thing the finding says is missing: "…every lever in the inventory would add to a package that is ahead of the stated position — the open question is whether the aim itself still reads right" (strategy_align.py:118-121). The Findings roll-up says it again: "sits above a deliberately lower aim — an aim-setting question rather than a delivery gap, taken up in its own section" (web/js/report.js:2300-2303). The cover tile already splits the count so the past rows do not inflate the alarm (report.js:1457-1463), and the register names "Past the stated aim" as its own state (report.js:2102). So a POINTER to the aim exists in three places. What does not exist anywhere is: a lever, a recordable decision, a plan action, an ask line, a risk entry, or any link to the place the aim is set. I checked all 37 levers in data/reward_levers.json — two registers only (22 Substance, 15 Approach), none of which re-sets a stated position; `_KIND_REGISTER`/`_registers_for` (strategy_align.py:76-94) has no third register. The word "aim" appears in an engine-authored recommendation exactly once, in that coverage_note. CORRECTION 2 — on a MIXED domain the document offers the wrong thing, not nothing, and the aim sentence vanishes. The renderer merges levers across all of a domain's option blocks (report.js:1894-1896, and the pagination twin at :980-982) and then renders the coverage_note only in the `else` branch when the merged list is empty (report.js:2073, mirrored at :2081). A domain with a past position gap AND a coherence gap therefore prints both ga

**Where it lands:**

- `server/strategy_align.py:112-122`
- `server/strategy_align.py:262-263`
- `server/strategy_align.py:76-94`
- `server/strategy_align.py:41`
- `server/strategy_align.py:356`
- `server/strategy_align.py:409-421`
- `server/positions.py:1329-1345`
- `data/reward_levers.json`
- `web/js/report.js:1894-1896`

**Risk:** Three things break, in rising order. 1. THE NON-DIRECTIVE CONTRACT. The document never instructs. `qa_strategy_align.py:214-215` asserts the past-aim note is clean against `DIRECTIVE_RE` and `LEGAL_RE`; qa_strategy_doc.py:313-315 sweeps the same regexes over the rendered text; `validate_plan` rejects a directive action. Any wording like "revisit the aim" or "reduce spend" trips them — correctly. Whatever is added must be a stated open question with the decision left to the reader, exactly as the current note is. Caught by: the existing DIRECTIVE_RE/LEGAL_RE checks, which already run. 2. THE FOUR-STATUS / COUNTS CONTRACT (R5). "past" is bundled inside `behind_intent` (strategy_align.py:41, :262-263), so it counts as a gap in `counts`, `gaps_total`, the cover, the ask, the register and the concentration risk. Un-bundling it fixes the count inflation the close-out read flagged and the 5 uns

**Depends on:** David's R8 sign-off on data/reward_levers.json (the _readme still carries DRAFT/pending-re; A ruling on whether 'past' stays inside behind_intent or becomes its own off-strategy clas; A ruling on whether the aim question may be a RECORDABLE decision (it would enter the_ask ; Re-rendering artefacts/total_reward_strategy_and_plan.pdf: the whole --pdf half of Gate A 

**Recommendation:** DO STEP 1 NOW; DEFER THE REST TO A RULING. Step 1 is a straight renderer defect with a gate already warning on it, on the exact page a remuneration committee reads. It needs no ruling — the ruled behaviour (a past gap gets no levers and states why) is what the engine already does; the renderer is failing to print the "states why" half. Ship it, flip qa_strategy_doc.py:686 from `advisory=True` to blocking once it passes, and while you are in there fix the qa_strategy_align.py:78-79 fixture to a lag aim with an above-market read so the engine half is exercised against a state production can reach. Step 2 (the pointer clause, and optionally the mirror risk) is cheap enough to ride the same arc 

**Cheapest honest version:** Two steps, and the first needs no ruling at all. STEP 1 (defect only, no ruling, one diff). Stop the renderer dropping the note. Render each option block's `coverage_note` beside the merged table rather than only when the merged list is empty — report.js:2073 and :2081, with the deck sentence at :1397-1400 changed from `every(g => g.direction === "past")` to `some(...)` so a mixed domain says both things. Nothing in the engine, the data model or the counts moves. This alone fixes the live Pay case: the aim question reappears on the one page where a committee is reading "you are overspending" next to two governance levers. It is also demonstrably failing right now — G62 WARNs on the shipped a

### Findings (§08) covers four areas of eight — establish what selects the cards, whether it is a rule or a prompt artefact, and what full coverage (including every

**Premise holds:** yes · **Size:** medium · **Needs a ruling:** yes

PREMISE HOLDS, and the cause is narrower than "a selection rule": §08 prints four cards because `server/strategy_diag.py:154` ends `compute_findings` with `return findings[:4]`. It is a hard truncation of a ranked list, not a coverage rule, and it has nothing to do with the diagnosis prompt. The real selection rule (strategy_diag.py:99-154) is: competitive domain (app.py:2572 passes `competitiveness`) AND a market verdict in below/at/above (line 107) AND `delta != 0` where delta = verdict_index − `domain_aims()` (lines 109-113). Everything surviving is scored (line 121: |delta|*10 + off-share*6 + £/10k capped at 8, ×1.6 when the objective matches the kind), sorted, then cut to four. The prompt is innocent. `DIAGNOSIS_SYSTEM` (claude_api.py:1739) forbids adding or dropping findings, and `validate_diagnosis` (claude_api.py:1815) rejects any count mismatch, shipping the deterministic floor. The model narrates exactly the N the engine hands it. The live org's cached row is `source: deterministic` anyway (metric_commentary, question_id `__diagnosis__`). I reproduced the shipped section exactly. Live strategy for org 5e67fa8c: market_position=lead, reward_mix=cash, p4p=moderate, objective=attract, domain_targets set for 6 of 8 areas (no Wellbeing, no Governance — they inherit "lead"). Feeding those aims plus the eight domain verdicts back through `compute_findings` returns the same four areas in the same order (Wellbeing, Incentives & Recognition, Pensions & Savings, Benefits & Lifestyle). Removing the `[:4]` returns SEVEN: gap Wellbeing (aim above / reads below) gap Incentives & Recognition (above / below) gap Pensions & Savings (on / below) gap Benefits & Lifestyle (on / below) gap Governance & Transparency (above / ON MARKET) ← dropped by the cap over Time Off & Family (below / on market) ← dropped by the cap over Pay (below / on market) ← dropped by the cap Only Health & Protection is genuinely on plan. So the section headed "where the declared strategy and the live position diverge" silently omits three of seven divergences — including the one area carrying the paper's only two Contradicted rows. Two things make this more than a one-line fix: 1. THE CAP IS LOAD-BEARING FOR A RULED SENTENCE. `deterministic_diagnosis` (strategy_diag.py:202) writes "N areas sit below market against the position you set", counting every kind=="gap" finding. Today all four gaps happen to read below market, so it is true. Uncapped it prints "5 areas sit below market" when only 4 are — and `validate_diagnosis` PASSES it (I ran it: `(True, None)`). Worse, the accurate wording is 

**Where it lands:**

- `/Applications/Lumi Project/server/strategy_diag.py:154 — return findings[:4], the whole cause`
- `/Applications/Lumi Project/server/strategy_diag.py:99-131 — the real selection rule (competitive + verdict present + delta != 0) and the evidence sent`
- `/Applications/Lumi Project/server/strategy_diag.py:35-70 — domain_aims: base stance, reward_mix/p4p nudges, domain_targets override`
- `/Applications/Lumi Project/server/strategy_diag.py:189-223 — deterministic summary; line 202 is the sentence the cap keeps true`
- `/Applications/Lumi Project/server/claude_api.py:1754-1759 — prompt rule 7, asserts the invariant the cap maintains`
- `/Applications/Lumi Project/server/claude_api.py:1836-1845 — D071 validator regex that bans the accurate uncapped wording`
- `/Applications/Lumi Project/server/claude_api.py:1815 — 1:1 count gate (why the prompt cannot be the cause)`
- `/Applications/Lumi Project/server/claude_api.py:1033 — DIAGNOSIS_VERSION (cache key); 1791 — DIAGNOSIS_GEN_VERSION is dead`
- `/Applications/Lumi Project/server/app.py:2567-2585 — the only inputs compute_findings gets; no commitments`

**Risk:** The headline risk is reintroducing D071 in mirror image, and nothing currently catches it: uncapped, the deterministic floor prints "5 areas sit below market" when 4 are, and validate_diagnosis returns (True, None) on that string — I ran it. The validator only bans the wide wording, never checks the narrow one against the finding set. Second: stale cached papers. Narrated findings are persisted per payload hash in metric_commentary('__diagnosis__') keyed on DIAGNOSIS_VERSION (claude_api.py:1033). Any change here must bump that constant, not DIAGNOSIS_GEN_VERSION, which is dead — otherwise readers keep the four-card section and the page count moves under them. Third: duplication. report.js:2296-2303 already tells the reader on p15 that Pay and Time off & family sit above a deliberately lower aim "taken up in their own sections". Surfacing them as over-cards puts the same fact twice on fac

**Depends on:** Close-out blocker 11 / the critic's root-cause item: Wellbeing and Governance & Transparen; D071 (claude_api.py:1836-1845 + prompt rule 7): its fix assumes every gap finding is also ; DIAGNOSIS_VERSION vs the dead DIAGNOSIS_GEN_VERSION (claude_api.py:1033 / 1791): fix or de; verify_report_pdf.py pagination re-run for the third Findings sheet.

**Recommendation:** DO IT, but split it and take the copy fix first — do not open with the engine change. Step 1 (ship now, small, report.js only): make the section's scope statement true. Either title it as the most material N with a derived "the other N divergences are covered in their own sections on pp X-Y" line, or drop the completeness claim from the standfirst at report.js:2272. This answers the actual charge — a section headed as complete that is silently partial — with no engine change, no validator interaction, no cache bump, no AI regeneration, and no reversal of a ruled fix. One diff, one ?v= bump. Step 2 (medium, needs David first): lift the cap to full gap coverage and fix the sentence that the ca

**Cheapest honest version:** Copy only, in one file: change report.js:2272 so the standfirst stops claiming completeness, and add one derived sentence naming how many divergences are covered elsewhere. Zero engine risk, zero validator risk, no cache invalidation, no reversal of D071 — and it closes the blocker as written ("headed as complete, silently partial"). If more is wanted but the wording reversal is unwelcome, the middle version is to uncap GAP-kind findings only and leave over-kind divergences to the p15 sentence and their own sections; that avoids the duplication question entirely but still requires the summary-clause fix, because Governance is a gap that reads on market.

### Arm C class: gate fixtures never exercise the healthy twin/group block path — build a shared cut/twin-block roster and adopt it across the gates

**Premise holds:** yes · **Size:** medium · **Needs a ruling:** no

PREMISE HOLDS, with two corrections that change the shape of the work. WHAT IS TRUE. Production builds a cut two ways and a twin-block dict one way: parse_cut (app.py:786-816) whitelists five dims and, for group, attaches label+criteria from peer_groups (app.py:792-800); twin_blocks_if_needed (app.py:819-836) returns peer_twin.twin_blocks for twin, peer_twin.group_blocks for group, `{}` + cut["too_small"] for a below-floor group (app.py:827-829), and None for every other dim; build_items (app.py:2291-2296) only calls it `if cut.get("dim") in ("twin","group")`. positions.py:83-111 has four resolvers (block_for / score_block_for / presence_block_for / matrix_row_block_for) whose twin/group branch reads twin_blocks["main"|"score"|"presence"|"rows"] — none of which any gate reaches with a populated dict. The direct-DB gates hand-build the cut and pass None: qa_overview.py:47 (`cut = {"dim":"all","value":None}`), 55-57, 71-72, and again hardcoded at 300 and 312; qa_domain_summary.py:60 (all + industry only); qa_hero.py:258 (`pos.practice_position_items("o", {"dim":"all"}, ...)`, tb argument omitted); qa_prevalence_rename.py:68. CORRECTION 1 — "never exercise group cuts" is false. qa_engine_audit.py:695-728 mints a real peer group over HTTP and requests `?cut=group&cut_value=<gid>` end to end, plus the foreign-id fallback at 723-726. It is the BELOW-FLOOR branch only (`{}` blocks, everything suppressed). Bigger: the demo org's default_cut is `group::76188216-...` (only org in the store with one), and /api/overview re-points the SIGNALS build to it at app.py:2401-2403 — `sig_items, sig_tb = build_items(request, org, user, sig_cut)` fires twin_blocks_if_needed and gets a real 344-key dict. So every HTTP gate that GETs /api/overview as director@thornbridge (qa_hero.py:61, qa_focus.py:56) already EXECUTES a healthy group cut inside production code. What is missing is any assertion on it — and qa_overview.py rebuilds that same org's signals on all-peers with tb=None, so it validates a set the product does not ship. Measured on a throwaway copy of live: all-peers 55 signals, org-default group 57 (4 group-only, 2 all-only), pool 263 both, 0 vs 2 of 328 blocks suppressed. CORRECTION 2 — TWIN is the genuinely untouched path, and it is where the risk sits. The only `?cut=twin` in the tree is server/verify.py:273, and verify.py:1-14 is DEPRECATED and sys.exit(2)s without --force; it is not in run_gates.sh. Measured on the demo org: twin gives 15 of 328 suppressed blocks (vs 0 all-peers, 2 group), 182 items vs 184, pool 258 vs 263, 36 signals vs 55. Twin is the only cut i

**Where it lands:**

- `/Applications/Lumi Project/server/app.py:786-816 (parse_cut)`
- `/Applications/Lumi Project/server/app.py:819-836 (twin_blocks_if_needed; :827-829 the below-floor `{}` + too_small)`
- `/Applications/Lumi Project/server/app.py:2291-2296 (build_items; :2294 the twin/group guard; request arg unused)`
- `/Applications/Lumi Project/server/app.py:2401-2403 (signals re-point to the org default cut — the live group exercise)`
- `/Applications/Lumi Project/server/app.py:2659-2672 (_org_sweep_cut; note the stale comment at 2649-2652 saying 'never twin ... or group', contradicted`
- `/Applications/Lumi Project/server/app.py:7170-7183 (build_commentary_payload's inline cut constructor)`
- `/Applications/Lumi Project/server/app.py:7355-7364 (domain-summary inline cut constructor)`
- `/Applications/Lumi Project/server/app.py:3915-3923 (dashboard-CSV inline group resolution)`
- `/Applications/Lumi Project/server/positions.py:83-111 (the four twin/group block resolvers)`

**Risk:** WHAT BREAKS. (1) False failures from cut-specific rulings looped over cuts. Measured instance: qa_hero.py:131 asserts `at_share < 0.70` ("hero not washed out"); on the demo org that is 0.58 all-peers, 0.54 group, 0.66 twin — a 4-point margin on a judgement threshold, not an invariant. Loop qa_hero naively and the suite goes red for no defect, which burns the demonstrated-failing-first discipline on noise. (I checked the other candidate: Wellbeing keeps its strict verdict and market_eligible=True on all three cuts, so qa_hero.py:200 is safe — that one was a false alarm.) (2) Silent half-threading. qa_overview.py:312 passes a literal None into pos.market_pool_qids while the prevalence items beside it would carry tb — a twin-cut prevalence pool compared against an all-peers market pool, green and meaningless. Every hardcoded None in an adopting gate must be found, not just the ones at the t

**Depends on:** ARM_C_SWEEP_2026-08-17.md row set (37 of 120 rows touch twin/group; the 26 figure is a cla; P0-7f fix staying in place — mutant (a), the strongest failing-first demonstration, is its; run_gates.sh GATE-COUNTS / NOT EXERCISED mechanism, for recording a skipped twin cut hones; demo org 5e67fa8c (Thornbridge) keeping default_cut=group::76188216 and a similarity vecto

**Recommendation:** DO A SMALLER VERSION NOW; DEFER THE ROSTER. The full item — new module, adoption across 3-5 gates, per-check cut-invariance triage, a mutant demonstrated failing per gate — is a medium with a long tail of judgement calls, and the sweep's own count (26-37 of 120 rows) overstates how much of it is one mechanical change. Do not buy it as one piece. Buy these two, in order, as one reviewable diff each: STEP 1 (highest value per line, ~30 lines, qa_overview.py only). Add a twin-cut pass around the cut-invariant subset — checks 1a/1b/1c, 5a, 7, 12a, 14a — built with `tb = A.twin_blocks_if_needed(conn, org, {"dim":"twin","value":None})`, skipping with a printed reason if it raises. Leave 11b/11c an

**Cheapest honest version:** qa_overview.py only, no new module: (1) a twin-cut pass over the cut-invariant checks (1a/1b/1c, 5a, 7, 12a, 14a) using A.twin_blocks_if_needed, skip-with-reason if the org has no similarity vector — ~30 lines, +0.15s, and it is the only twin-block coverage that would exist anywhere in the tree; (2) rebuild the signals half on A._org_sweep_cut(conn, org) with its real tb, so the gate asserts the 57-signal group set the product actually ships for that org instead of a 55-signal all-peers set it does not — ~15 lines. Together these buy the two paths that carry the risk, need no cross-gate coordination, and leave every cut-specific ruling (qa_hero's wash-out threshold, the render greps) untouch

### F-000 — the reward document's footer total does not survive a non-default peer cut

**Premise holds:** yes · **Size:** medium · **Needs a ruling:** yes

THE BLOCKER IS REAL AND WORSE THAN LOGGED — BUT THE STATED CAUSE AND SCOPE ARE BOTH WRONG. Reproduced at HEAD (7914424) on a throwaway DB pair, live stored plan, the canned-payload harness, headless Chrome, `server/verify_report_pdf.py`: | cut | cut_label (chars) | physical | claimed | failures | |---|---|---|---|---| | all peers | "All peers" (9) | 38 | 38 | 0 | | fte_band | "50-249" (6) | 38 | 38 | 0 | | twin | "your peer group" (15) | 38 | 38 | 0 | | **industry** | "Retail & Consumer Goods" (23) | **39** | 37 | **7** | | **group** | "5 sectors · 10,000+, 1,000-4,999 FTE" (36) | **40** | 38 | **7** | Three corrections to the premise: 1. **"A group cut produces more content" is false.** I ran the group-cut payload UNCHANGED with only `cut_label` rewritten to "All peers": **38 pages, 0 failures.** Same data, same signals, same gaps, same levers. Then the same payload with a 60-character label (the API's own cap, app.py:4908/4926): **41 pages, 3 spills, 8 failures.** The variable is the LENGTH OF THE PEER-GROUP NAME STRING, nothing else. The group cut does not carry more content; it carries a longer customer-typed name. 2. **"Group only" is false.** The industry cut fails too, and fails earlier — its first spill is §08 *The commitments in full*, and the contents starts mis-targeting at "Findings" rather than deep in Part B. Any label longer than "All peers" is a candidate. `FORMAT_AUDIT_2026-08-17.md:36` and `CLOSE_OUT_2026-08-17.md:85` both record "YES — group only"; both are wrong. 3. **"41 against 40, §16" is stale.** Those numbers came from `p07f_group.pdf` rendered 17 Aug 11:16, six commits before HEAD. At HEAD it is 40 against 38 with TWO spills (§12 Health & protection, §17 Governance & transparency), not one. THE MECHANISM. `TOTAL = pages.length` (report.js:1070) is a hand-derived count decided before the browser lays anything out, and it is printed as "Page N of M" on every sheet (report.js:150). Each logical sheet is fitted to A4 by budgets that count ITEMS, never rendered height — `chunk`/`Prun` (report.js:869-885) weigh with constant functions; `_room` (1914), `followSheets` (988) and the per-run budgets (959, 1018, 1028, 1040, 1043) are hand-measured integers. `cutLabel` is then interpolated into four sites that sit INSIDE those fitted regions: the domain deck sub (1928), the domain signals exhibit caption (1118), the domain commentary prose (1364) and the register's READ FROM cell (2104). A long label wraps each of those, adding 1-2 lines to sheets already measured at 89-97% full. `.pack-page { min-height: 296mm }` with `page-break-after: al

**Where it lands:**

- `/Applications/Lumi Project/web/js/report.js:1928 — domain deck sub interpolates cutLabel into a fitted sheet`
- `/Applications/Lumi Project/web/js/report.js:1118 — signals exhibit caption interpolates cutLabel`
- `/Applications/Lumi Project/web/js/report.js:1364 — domProse interpolates cutLabel into the commentary`
- `/Applications/Lumi Project/web/js/report.js:2104 — register READ FROM cell prints 'Market read on ' + cutLabel in a narrow column`
- `/Applications/Lumi Project/web/js/report.js:869-885 — chunk/Prun budgets weigh items, never rendered height`
- `/Applications/Lumi Project/web/js/report.js:1914 — _room, the per-sheet signal budget (comment says '2, not 3'; the code says 1 — stale since 477afe4)`
- `/Applications/Lumi Project/web/js/report.js:1070 — TOTAL = pages.length, asserted before layout`
- `/Applications/Lumi Project/web/js/report.js:150 — the 'Page N of M' claim`
- `/Applications/Lumi Project/web/css/app.css:2624-2626 — min-height 296mm + page-break-after: the spill path`

**Risk:** WHAT COULD BREAK. 1. The per-sheet basis statement weakens. "How X sits against <peer group>" and "reads against <peer group>" were added deliberately in the external-review era so a reader holding one sheet knows what it is measured against. Removing the label from those four sites trades that for fit. Mitigation already in place: the running footer prints "Peer group: <label>" on EVERY sheet (report.js:836), and the cover and Method and basis both state it in full — so the fact is not lost, only de-duplicated. 2. Gate A's string anchors move. qa_strategy_doc.py's PDF half greps the artefact for exact sentences (A2.1 provenance, A1.1-A1.3 register frame, A4.5/A4.7 leak checks). Changing domain deck subs and captions will not touch those, but the register cell change might; the whole --pdf arm has to be re-run against a fresh artefact, not assumed. 3. The opposite failure goes undetected

**Depends on:** A David wording ruling: whether the peer group may be named once (cover, running footer, M; R-h, the format-convention ruling still open in FORMAT_AUDIT_2026-08-17.md — F-001 (four c; A ruling on whether the under-55%-full measure becomes blocking. Without it, a fix that pa

**Recommendation:** DO IT — it is a genuine ship blocker on every cut but the default, and it is smaller than it looks now that the cause is isolated to one string. Do the SMALLER VERSION below, not a budget retune. Retuning `_room` or the chunk budgets cannot be proven correct: the input is a customer-typed name of 1-60 characters whose wrapped height is not even monotone in its length, and the budgets have already been retuned five times in this file's history (the comment at report.js:1908 still says "2, not 3" against a constant that reads 1 — the tuning has visibly drifted out of sync with its own reasoning). Every retune is one org's label away from the next red gate. Do NOT do the full engine version (me

**Cheapest honest version:** TAKE THE VARIABLE-LENGTH STRING OUT OF THE FITTED REGION, AND LEAVE THE FITTING MODEL ALONE. The peer group is already stated in full three times where nothing is tightly fitted: the cover, the running footer on every sheet (report.js:836) and Method and basis. Inside the four fitted sites — report.js:1928, 1118, 1364, 2104 — replace the interpolated label with a fixed-length referent ("your peer group", "the comparison pool", "the market read"). Every domain read sheet and the register then have a height independent of what the customer named their group. This is PROVEN, not estimated: the group-cut payload with the label alone shortened renders 38 pages, 0 failures — identical to all-peers

### Family-provision contradiction: Part A prose ("deliberately more generous than the market") vs the register ("You aim below market on Time Off & Family"), uncau

**Premise holds:** yes · **Size:** small · **Needs a ruling:** yes

The premise holds, but the sentence is NOT prose — and the "COULD a rule catch intent-vs-intent" question is already answered YES by shipped code. 1. WHERE THE PART A SENTENCE COMES FROM. It is a captured enum field, not model invention. `family_position` is a three-value dial (`statutory|market|over`) declared at server/app.py:5061 and stored at server/db.py:633. Its label string is hardcoded at server/app.py:5084-5086 — `"over": "deliberately more generous than the market"` — verbatim the sentence in the finding. It is injected into the statement payload as `dials.family_position` at server/app.py:5596-5597, and printed by BOTH paths: the deterministic floor composes `"Family provision is %s." % d.get("family_position")` at server/claude_api.py:950, and the AI path receives the same pre-labelled string and is forbidden from adding to it (STATEMENT_SYSTEM hard rule 1, server/claude_api.py:884-886). So the sentence is derived from a captured field on every path. AI on/off is irrelevant. The same dial also prints as "Family-friendliness: Generous" in the Exhibit-1 dials table (web/js/report.js:1719, label from web/js/strategy.js:44). 2. WHERE THE REGISTER SENTENCE COMES FROM. server/strategy_align.py:270 — `"You aim %s on %s; %s."` — with the stance from `dt.get(name) or global_stance` (line 256), i.e. `domain_targets["Time Off & Family"]` falling back to the global `market_position` dial (line 247). 3. THE ROOT CAUSE. Two INDEPENDENT captured dials both state an intent about family generosity — `family_position` and `market_position`/`domain_targets["Time Off & Family"]` — and no code path reconciles them. Nothing is derived wrongly; the data model simply lets a member set "over" on one dial and "lag" on the other, and prints both. The capture UI reinforces the split: web/js/strategy.js:44 tells the member a generous family position "reads as on strategy — intended, not overspend", with no reference to the area aim. 4. CAN A COHERENCE RULE CATCH INTENT-VS-INTENT? YES — the grammar already exists and is committed. server/strategy_align.py:201-214 implements a `strategy_stance` evidence clause that reads the effective stance for a domain (domain_targets override, else the global dial) instead of a metric answer. The precedent rule P6 (data/strategy_coherence_rules.json:182-201, "attract-led objective vs a below-market pay aim") is exactly this shape and shipped in commit fb52ee6. The file's own _readme (line 2) documents the extension: "a strategy can pull against itself and no answer can evidence that". 5. I PROVED THE FIX IS DATA-ONLY. Appending one rule

**Where it lands:**

- `data/strategy_coherence_rules.json:182-201 — P6, the shipped intent-vs-intent precedent and the append site`
- `data/strategy_coherence_rules.json:2 — _readme documenting the strategy_stance extension and David's ownership`
- `server/strategy_align.py:201-214 — the strategy_stance evaluator (no change needed)`
- `server/strategy_align.py:270 — the register sentence 'You aim below market on Time Off & Family'`
- `server/strategy_align.py:110-121 — the overspend coverage-note guard, the precedent for suppressing levers`
- `server/app.py:5061 — family_position enum`
- `server/app.py:5084-5086 — the literal 'deliberately more generous than the market' label`
- `server/app.py:5596-5597 — family_position injected into the statement payload`
- `server/claude_api.py:950 — the deterministic floor's mix sentence`

**Risk:** WHAT COULD BREAK. 1. Live content change across the estate. The rule fires on every org with family_position="over" and a lag aim on Time Off & Family, adding a "contradicted" row to already-approved documents. That is the correct outcome, but it is visible movement in signed-off artefacts. 2. Count drift. Each fired rule adds one commitment to the Time Off & Family denominator and to the category rollup counts printed on the register. server/qa_strategy_align.py:137 asserts counts reconcile (safe — it is a sum check, not a literal), but server/qa_strategy_doc.py:659 parses bare gap counts out of rendered prose and is the place a count change would surface. 3. The silent-typo trap (caveat c above): a mis-cased domain key passes every existing check and reads the global dial instead of the override. Nothing today catches it. 4. Lever mismatch (caveat b): shipping the rule without the inte

**Depends on:** David's R7 sign-off on the rule statement/commitment/rationale text — the file is explicit; A decision on whether to ship the intent-conflict coverage note (suppressing levers) at th

**Recommendation:** DO IT, as the data-only version, plus the two gate blocks — but take David's ruling on the rule TEXT before shipping, because data/strategy_coherence_rules.json is explicitly David-owned (its _readme line 2) and every rule in it including P6 is still marked "pending David's R7 sign-off". The mechanism is a defect fix and needs no ruling; the sentence that prints in a board document does. Ship it as TWO rules, not one, for the same cost — the mirror is otherwise asymmetric and invites the reverse complaint: F1: family_position="over" + Time Off & Family aimed "lag" -> contradicted F2: family_position="statutory" + Time Off & Family aimed "lead" -> contradicted Both are pure JSON; I verified t

**Cheapest honest version:** The data-only rule IS the cheap version and it is genuinely cheap — one JSON object into a hot-reloaded, David-owned file, zero engine change, proven working against the unmodified engine. There is no meaningfully cheaper honest option below it. What you CAN defer, and should: - Caveat (b), the lever mismatch. Suppressing levers for an intent-conflict needs a new `direction: "intent_conflict"` flag set in server/strategy_align.py:293-299 and a matching branch beside the overspend guard at lines 110-121, plus its own coverage note. That is a second small diff with its own gate, and it should be scoped together with P6, which has the identical defect today and shipped that way. Deferring it le

### Exhibit 2's aim marker: a three-word stance plotted as a single percentile point (RR_AIM_PCT, web/js/report.js) — band/zone rendering, feasibility, and the exhi

**Premise holds:** yes · **Size:** small · **Needs a ruling:** yes

PREMISE HOLDS, and it is worse than reported — but it is also already diagnosed, already ruled-pending, and already gated red. What the code does, verified: - `RR_AIM_PCT` (report.js:801-802) turns the stance word into three literal percentiles off the live band: with LUMI_MARKET_BAND defaulting to "35-65" (app.py:108-109), lag = 17.5, match = 50, lead = 82.5. One consumer only (report.js:2225); drawn as a ~9px triangle at exactly that x (report.js:425-427). Six of eight rows carry one — the marker reads the commitments register (report.js:796-797), and Wellbeing + Governance are pinned out by STRATEGY_POSITION_EXCLUDE (app.py:5114). - The engine never converts a stance to a percentile at all. `_market_target` (positions.py:1329-1345) maps stance to an ORDINAL {lag:0, match:1, lead:2} and compares it against the domain VERDICT ordinal {below:0, at:1, above:2}. Alignment (behind/on_target/ahead) falls out of that ordinal comparison and nothing else. - And the verdict is not the percentile. `_pool_verdict` (positions.py:954-981) sets the verdict from `lean = (above-below)/n` over metric-level bands, with a wide dead zone (VERDICT_NET_LEAN = 0.25, app.py:135 / gen_market_position_config.py:40), while `depth_pctl` — the dot's x (report.js:2214, 429) — is the MEDIAN polarity-adjusted percentile. Two independent measures; the document already says so in its own note (report.js:2255-2259, F-014). So the defect is not only false precision. The aim is plotted on an axis that has no defined relationship to the alignment it appears to be testing. Two areas at an identical depth percentile can be on_target and behind — the ruling sheet records exactly that from live data (Governance P32 "on market" beside Wellbeing P32 "below", REVIEW_RULING_SHEET_2026-08-16.md:66-74). The band is arithmetically already there: 17.5 / 50 / 82.5 are precisely the midpoints of [0,35], [35,65] and [65,100]. The constant is a zone renderer that only draws the zone's centre. Feasibility in the existing SVG: yes, trivially, client-only. Same `rrPX` helper (report.js:333-336), 426px of inner width, 38px row pitch, and the marker already sits at cy-9, clear of the dot at cy — a bracket is one `<line>` plus two end ticks, or a stroked `<rect>`. CSS homes exist (.rrc-aim app.css:5353; legend swatch .rrc-k-aim app.css:5372-5373). No payload change, no server change. `RrDomainChart` (report.js:293) already destructures a `stance` prop and never uses it — a dead parameter, and the natural second home if the per-area ruler should carry an aim too. Already held work, not a new finding: Gate A check

**Where it lands:**

- `web/js/report.js:801-802 — RR_AIM_PCT, the three literal percentiles`
- `web/js/report.js:2225 — the only consumer, register-gated`
- `web/js/report.js:425-427 — the triangle path in RrPortfolioChart`
- `web/js/report.js:2248 — legend swatch 'where your strategy aims'`
- `web/js/report.js:2255-2259 — the F-014 two-measures note the exhibit note extends`
- `web/js/report.js:1103 — exhibit caption 'with the position the strategy sets for each'`
- `web/js/report.js:293 — RrDomainChart's unused `stance` prop`
- `web/js/report.js:1383-1390, 1953-1955 — the aim prose and the 'against your aim' stat (both ordinal, both correct today)`
- `web/js/report.js:2102 — the register's 'Short of the stated aim' status row`

**Risk:** The bracket makes a contradiction VISIBLE that the point marker was accidentally hiding, and the drafted ruling does not address it. With a "lead" bracket spanning [65,100], an area whose dot sits at P70 but whose metric mass leans "at" (reachable at margin 0.25) renders INSIDE its own aim bracket while the same document prints "the live read sits short of that aim" (report.js:1387-1390) and a register row reading "Short of the stated aim" (report.js:2102). Today the point at 82.5 sits to the right of that dot, so the picture agrees with the prose by luck. This is a fresh F-014-class picture-vs-prose defect on the one exhibit a committee actually looks at, and it must be settled before the diff, not after. Second risk: print/colour collision. A shaded aim ZONE would repeat the exact mistake the 2026-08-17 chart QA already fixed twice — .rrc-fill was deleted for sharing --favourable-tint 

**Depends on:** RULING R-B (REVIEW_RULING_SHEET_2026-08-16.md:55-64) — David's, still open; the gate is ad; RULING R-C (REVIEW_RULING_SHEET_2026-08-16.md:66-82) — dot position vs dot colour on the s; A prior decision on whether the aim may be rendered on the depth-percentile axis at all, g; Gate A vacuity repair for A5.3 (VACUITY_SWEEP_2026-08-17.md:809) — ships with whichever fo

**Recommendation:** Do it — but do the subtraction, not the bracket, unless David rules otherwise, and settle the axis question before any diff is written. Reasons: (a) the work is already scoped, gated and ruling-drafted, so the marginal cost is small either way; (b) the bracket is the more informative rendering but it buys a new picture-vs-prose contradiction on the document's most-read exhibit, and that contradiction class has already cost this artefact three separate hand-fixes; (c) the aim is already stated correctly, twice, in ordinal terms that assert nothing about percentiles — the "against your aim" stat (report.js:1953-1955) and the "You aim … here; the live read sits short of that aim" sentence (repo

**Cheapest honest version:** The cheapest honest version is a DELETION, and it is smaller than the bracket. Drop the aim from the ruler entirely: remove `aim:` from the row payload (report.js:2225), the triangle path (report.js:425-427), the constant (report.js:801-802), the legend swatch (report.js:2248) and its CSS (app.css:5353, 5372-5373). Roughly a dozen lines removed, nothing added. The chart then plots exactly one measure per channel — position by x, verdict by colour — which is also what the F-014 note at report.js:2255-2259 already claims it does, so that note SHRINKS rather than needing a third sentence. The aim keeps travelling in the stat and the sentence, both ordinal and both already correct. Zero chance o

### The priced figure's population is unstated — "about £X would move employer pension contribution to the peer median" carries no level population and no pointer t

**Premise holds:** yes · **Size:** small · **Needs a ruling:** no

PREMISE HOLDS, but the finding mislocates the code and overstates one clause. Two corrections first. (1) The sentence is NOT composed in report.js's cost section. It is composed server-side in the strategy-DIAGNOSIS engine: `server/strategy_diag.py:136` — `ev += "; about £%s would move %s to the peer median" % (opp["top_gbp"] or money, opp["top_label"].lower())`. That string becomes the finding's `evidence`, which becomes the card `detail` (deterministic floor at `strategy_diag.py:223`; the model path narrates the same payload). It renders in the report's Findings cards at `web/js/report.js:2284` — a different section from "What it costs" (`report.js:2593`). (2) "No basis" is only half right. The same PDF's What-it-costs section already states the basis in full — FTE-band midpoint, median salary, comparator cut, rounding convention (`report.js:1272-1285`, `report.js:2626-2639`). What the £X sentence lacks is the POPULATION and any pointer to that basis. Now the substance, which is real and is the same defect the engine already ruled on once. The figure is a whole-question SUM across levels. `positions.py:625-651` walks every matrix row, prices `gap_pp × salary × (fte × level_share)` per level and accumulates into `tot50`; `to_p50_gbp` is that sum. Rows are included only where the org entered a value AND the peer block is unsuppressed (`positions.py:628-631`), and EST rows are excluded (`positions.py:626`). So the honest population is exactly "the N of M levels you entered that have a peer median" — which the engine already computes and emits as `levels_covered` / `levels_total` at `positions.py:664-665`, under a comment recording the 2026-08-16 ruling verbatim: "a Board-only gap must never carry an org-wide £ silently". That ruling was implemented on ONE surface. The strategy-document payload carries the pair (`app.py:6109`) and the cost table prints it (`report.js:2619-2621`). The diagnosis payload does not: `app.py:2574-2584` rolls `money["items"]` into `opp_by_domain` keeping only `{gbp, direction, top_label, top_gbp}` — `levels_covered`/`levels_total` are dropped at the roll-up, so `strategy_diag.compute_findings` cannot state the population even if it wanted to. The data exists two frames up the stack and is thrown away. The ambiguity the finding describes is therefore live and is worse than stated, because the neighbouring signal is genuinely single-level. `data/signal_lenses.json` has `money_lenses: {}`, so the money branch of the signal builder (`signals.py:538-553`) never fires. The pension metric reaches the document only through `position_lens

**Where it lands:**

- `/Applications/Lumi Project/server/strategy_diag.py:132-138`
- `/Applications/Lumi Project/server/strategy_diag.py:223`
- `/Applications/Lumi Project/server/app.py:2574-2585`
- `/Applications/Lumi Project/server/positions.py:625-655`
- `/Applications/Lumi Project/server/positions.py:662-665`
- `/Applications/Lumi Project/server/app.py:6105-6111`
- `/Applications/Lumi Project/web/js/report.js:2282-2288`
- `/Applications/Lumi Project/web/js/report.js:2612-2625`
- `/Applications/Lumi Project/server/signals.py:596-614`

**Risk:** Three things can break, in ascending order of subtlety. 1. THE MODEL DROPS THE CLAUSE. If the fix is only `strategy_diag.py:136`, the deterministic floor states the population and the AI narration may not — `validate_diagnosis` (`claude_api.py:1808-1853`) checks numbers, counts, directives, legal language and two specific wordings, but nothing requires a population clause to survive narration. On the live AI rig the sentence would silently revert to the unqualified form. Caught only by a validator rule plus a rig-on run; a deterministic-only gate run (`ANTHROPIC_API_KEY=''`, which is what run_gates.sh forces) will show green over it. This is the failure mode most likely to ship. 2. NUMBER-GROUNDING WIDENS. `_diagnosis_numbers` (`claude_api.py:1794-1805`) allowlists every number found anywhere in the payload JSON. Putting "3 of 7 levels" into `evidence` automatically legalises 3 and 7 for

**Depends on:** A rendered artefact at artefacts/total_reward_strategy_and_plan.pdf (or LUMI_DOC_PDF) — wi; An AI-rig run with a live key for step 4: run_gates.sh forces ANTHROPIC_API_KEY='' so the ; DIAGNOSIS_GEN_VERSION / DIAGNOSIS_VERSION bump discipline (claude_api.py:1791, claude_api.; web/js ?v= cache-bust bump for any report.js change

**Recommendation:** DO IT, in the cheap form, before the wider form. This is not a new behaviour: it is the 2026-08-16 ruling at `positions.py:662-663` applied to the second surface it was always about. The engine already computes the population; one roll-up throws it away and one document section never asks for it. Sequence I would run: STEP 1 — gate first, failing. Add to `server/qa_strategy_diag.py` (pure unit, no DB, no server, ~2 minutes to run): extend the `OPP` fixture at line 40-41 with `levels_covered: 1, levels_total: 7` and assert that a finding whose £ rests on a partial population states that population in its `evidence`. It fails today. Add a second check to `server/qa_strategy_doc.py` scanning th

**Cheapest honest version:** CLIENT-ONLY, ZERO ENGINE CHANGE. The strategy-document payload already carries `levels_covered`/`levels_total` per priced item (`app.py:6109`), and the Findings cards render in the same document as the cost table. So `report.js:2282-2288` can, for each finding whose detail names a priced metric, look that metric up in `money.items` by its payload label and append one deterministic line: "Employer pension contribution is priced on the 1 of 7 levels you have entered — see What it costs." That is roughly five lines of JS plus a `?v=` bump. It buys the population AND the cross-reference, it cannot be dropped by the model because it is rendered outside the model's text, it needs no cache-version 

### D088 — the "most material is not most representative" caveat is gated on rows printed, not rows existing, so an area whose exhibit is folded to zero rows can ne

**Premise holds:** yes · **Size:** small · **Needs a ruling:** yes

PREMISE HOLDS, and the code is exactly as described — with two refinements worth more than the finding itself. The mechanism, verbatim. `web/js/report.js:1914`: `const _room = inlineFollow ? 0 : 1 - (cntStrictFalse ? 1 : 0);` then `:1915` `const sigShown = (b.signals || []).slice(0, Math.max(0, _room));`. The caveat at `:2034` reads `pos.verdict && sigShown.length && !sigShown.some(x => x.position === pos.verdict)` — and it is not merely gated on `sigShown.length`, it is lexically INSIDE the `(sigShown || []).length ? html\`…\` : null` branch opened at `:2004` and closed at `:2049`. So when `_room === 0` the caveat is unreachable twice over. What renders instead is `:1999-2003`, which says only "X is flagging N signals; they are set out in full under X in the app" — no statement about whether any of them agrees with the verdict. That string is confirmed in the rendered artefact at p24 (`CLOSE_OUT_READ_2026-08-17.md:29`, `:151` — "One example is printed per area except Time off, which shows none"), and the ruling is recorded at `DECISIONS.md:20047` as renderer change, not authorised, recorded not fixed. REFINEMENT 1 — it is a class of two, not one area. `inlineFollow` (`:1890`) is `isRead && gaps.length && (parts||1)===1`, and `parts` is 1 only when `hasFollow` is false (`:982`, `:989`), i.e. gaps > 0 AND levers = 0. That is Time off & family. But `_room` also reaches 0 down the OTHER arm: a non-inline-follow area that is INDICATIVE (`cntStrictFalse`, `:1891`, from `market_eligible` at `server/app.py:6028`, rendered as the "* indicative" note at `:1965`) gets `1 - 1 = 0`. That second member is latent in the current document (no area is indicative there) but it is live code — and it carries a second, separate defect: the exhibit walk at `:1116-1118` mirrors only the inlineFollow half of the room rule and does not check `strict`, so an indicative area with signals would be allocated an exhibit number for a table that never prints. That is precisely the hole the comment at `:1113-1115` was written to prevent ("it skipped 5 and 10"). Any fix that touches `_room` must keep `:1116` in lockstep, and the cleanest fix does that by construction. REFINEMENT 2 — the truncation does NOT make a client-side fix dishonest, and this is what keeps the work small. `server/app.py:5978-5988` sorts each area's signals verdict-agreeing-first (`_lst.sort(key=lambda x: 0 if x.get("position") == _v else 1)`, a stable sort over the materiality order) BEFORE `app.py:6047-6053` slices to `_sigs[:4]`. So if any of the nine agreed with the verdict it would be `b.signals[0]`. Testing `(

**Where it lands:**

- `/Applications/Lumi Project/web/js/report.js:1914 — the _room rule (both zero-arms)`
- `/Applications/Lumi Project/web/js/report.js:1915 — sigShown slice`
- `/Applications/Lumi Project/web/js/report.js:1999-2003 — the folded-branch sentence that must carry the clause`
- `/Applications/Lumi Project/web/js/report.js:2004 — the ternary the caveat is trapped inside`
- `/Applications/Lumi Project/web/js/report.js:2034-2042 — the caveat, gated on sigShown.length`
- `/Applications/Lumi Project/web/js/report.js:1890 — inlineFollow`
- `/Applications/Lumi Project/web/js/report.js:1116-1118 — exhibit walk, mirrors only half the room rule`
- `/Applications/Lumi Project/server/app.py:5978-5988 — agree-first sort (what makes the 4-row test faithful)`
- `/Applications/Lumi Project/server/app.py:6036,6047-6053 — signal_count vs signals[:4]`

**Risk:** See above — layout on the tightest sheet in the document, caught by verify_report_pdf.py:70-96; plus two gate traps in the new copy (D072's cross-page 700-char window, A4.5/D069/A4.7 prose checks); plus the exhibit-walk lockstep at report.js:1116-1118 if _room is touched, which no gate covers.

**Depends on:** D087 — the deferral ruling (43 of 55 signals named but unprinted, PHASE2_REPORT_2026-08-17; The standing ruling at DECISIONS.md:20047 — 'Renderer change, not authorised, recorded not; One genuine design choice for David: does the clause fire only when zero signals agree (ma; server/app.py:5978-5988 — the agree-first sort. It is load-bearing for a client-only fix a; report.js:1116-1118 — the exhibit walk must stay in lockstep with any change to _room; no 

**Recommendation:** DO IT, in the smallest form, once authorised — but authorise the smaller of the two designs, not the tidy one. The cleanest fix by structure is to hoist one `const sigAgrees = pos.verdict && (b.signals || []).some(x => x.position === pos.verdict)` above the template and render a single caveat paragraph outside the exhibit-card ternary, with three wordings (one row shown, several shown, none shown). That is genuinely cleaner: one condition, one source of truth, and it makes the printed and folded cases obviously the same fact. But it restructures the `:2004-2049` ternary, moves the paragraph D072's 700-character window is currently reading, and touches the one sheet in the document with the l

**Cheapest honest version:** Edit only the folded-branch sentence at `report.js:2000-2003`, and fold the clause INTO the existing sentence rather than adding a paragraph: "… is flagging 9 signals; none of them reads on market, and they are set out in full under Time off & family in the app." That is one `const` plus one string change, about +35 characters, likely zero extra printed lines, and it does not touch the exhibit card, the `_room` rule, the exhibit walk, or D072's scan window at all. Layout risk drops to near nothing while the reader gets the entire substance of the warning. A cheaper-still variant exists for the GATE rather than the fix, and it is worth considering on its own merits: make the clause unconditio

